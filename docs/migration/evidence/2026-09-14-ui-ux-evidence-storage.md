# 运行截图迁出与未发布历史清理

日期：2026-09-14。用户在合并推送过程中明确提出截图不应提交Git。本次中断正在上传的推送，并通过 `git ls-remote` 确认远端 `ubaa2` 仍为 `347eda209cda9f5a85804e8fffb93e84ab489188`，新提交没有进入远端分支。

清理前终点为 `b5d35422e6f30f0f2e24b612c2b0f3185330cd7b`，相对远端有40个未发布提交。已先将整个 `docs/design/evidence/` 归档到仓库外，8,001个文件逐文件SHA-256核对一致，其中3,981张截图共1,397,627,832字节。原两个本地分支的未发布历史另存Git bundle，并通过 `git bundle verify`；归档标识为 `UBAA-ui-ux-20260914`。

随后只对 `347eda209cda9f5a85804e8fffb93e84ab489188..ubaa2` 运行路径过滤，移除 `docs/design/evidence/` 下的图片和录屏。保留全部40个提交、原提交信息和分批结构，未对已发布基点或其他分支的已发布历史进行改写。清理后原终点为 `0e14d5144db3e9537ec335b8aa59b24f9585d1a3`，完整旧新关系见[提交映射](2026-09-14-ui-ux-commit-map.txt)。后续规则文档独立提交。

已对40组原/新提交逐一比较：差异仅为验收媒体路径，每个新提交都不含这些媒体；源码、测试、工程图片、JSON元数据和文字记录均保持对应版本的原内容。历史PNG清单、截图摘要与旧SHA保留采集时语义，不批量改写为新的验收结果；查阅方式见[证据规则](../../design/evidence/README.md)。

`.gitignore`、`AGENTS.md`、活动合同与实施清单已统一为仓库外保存截图/录屏，仓库内保留原型源文件、文字、元数据、摘要和索引。截图驱动已有 `UBAA_UI_EVIDENCE_DIR` 参数，后续必须指定仓库外的私有绝对目录。应用图标、图书馆地图及26张golden测试基准继续保留。

本次没有修改产品代码，也未恢复目标实施。原723项Flutter、Rust/CLI检查及Android构建针对的源码字节保持不变；本轮验证集中在归档完整性、提交范围、代码保全、截图排除、忽略规则与敏感内容。真正的设备相机、系统选图、最终三端和双路线只读等未完成项继续保留。
