# 全功能 UI/UX 执行证据

更新：2026-09-08，Asia/Shanghai。活动合同：`goal.md`。

## 续跑位置

- 已完成：P0 基线提交 `5fb00f33`；P2 全设计与54张原型自评提交 `e93d569e`；P3 状态连续性/巡检合同修复及72张原生手机平板截图提交 `b2b91d46`。
- 当前：B1已提交 `83ee9e89`；B2学期/教室选择器、分组宽表已实现并通过492项Flutter门禁，本批手机/平板各14业务场景及110/122图全部复核通过。
- 当前接续：B2已提交0f3803ca；P4-A资料/设置/手机查询折叠已通过500项门禁及原生完整/补充场景，79图已复核；接续P4-C/D与P5–P7。
- 阻塞：macOS CUA 再次报告 environment_locked，无法完成当前人工窗口/键盘验收，不绕过锁屏。图书馆公开 DTO 无完整跨日期时段关联，依来源核对仅实现已证首日选择，其余原能力保留。Core-live基线Direct40PASS/WebVPN38PASS+2NA；生产App未验，P6三设备最终验收未闭合。

## P0 基线

| 项目 | 本轮事实 |
|---|---|
| 开始源码 | `e6e0e3c5f9c52f9c302742a780da3e67a8251a65` |
| 开始状态 | `ubaa2...origin/ubaa2 [ahead 1]`；无未提交修改 |
| 工作区 | `/Users/moorefoss/Code/UBAA`，新建 `codex/ui-ux-redesign` 分支；复用已有锁定依赖、冻结目录与构建缓存，按合同允许的工作分支原地执行 |
| 引用 | `just refs`，退出 0；旧版 `6e75e120a26b0eefb3ab4a6f8251d1230db4a62e`，示例 `efb7976bf513f38364b88aeb83d704586cff9b2a`；纯校验 |
| 官方 Flutter | `bash scripts/check/flutter-toolchains.sh official`，退出 0；3.41.9 / `00b0c91f06209d9e4a41f71b7a512d6eb3b9c694` |
| Rust | `rustc 1.95.0 (59807616e 2026-04-14)` |
| 系统 | macOS 26.6.2 / 25G83 |
| 手机 | iPhone 17 Pro；`B196C00E-FDA6-45C2-BAC5-D7208B8D9DBD`；iOS 26.5 / 23F77；simctl 已确认 Booted，App 尚未验收 |
| 平板 | iPad Pro 11-inch (M5)；`9F9D9C27-D085-467E-86B6-2960D0082ECC`；同一 iOS runtime；Booted，App 尚未验收 |
| Flutter 基线 | `just flutter-check`，退出 0；domain 25、platform 48、app 189、ui 96、bindings 15、host 20、flutter 3，共 396 个测试；所有包 format/analyze 通过 |
| 基线日志 | 本机 `/tmp/ubaa-ui-ux-20260908/p0-flutter-check.log`，仅确定性输出 |
| 人工巡检入口 | `integration_test/app_flow_test.dart` 显式 `UBAA_UI_INSPECTION=true`；复用 `_AllWritesIntegrationBackend` 与 `MemoryCredentialVault`；默认生产入口不读取开关、不降级 Demo |

## 证据分层

基线 widget/golden 的 396 个测试不是原生设备或实时上游结果。人工巡检与宿主 integration 使用合成数据和 typed fake，不访问真实业务；真实只读仅走安全凭据路径并单独登记。真实学校写入始终不执行。

后续场景记录须含：编号、功能编号、源码、系统/设备、逻辑尺寸/DPR、主题、backend、步骤、预期/实际、截图相对路径、日期、结论。未执行、失败、阻塞与不适用分别记录。

## P0 原生运行与前置审查

- macOS Debug 原生宿主：显式 `_InspectionBackend`，所有数据为合成；CUA 已实际操作登录、十二领域主页、博雅长详情滚动→prepare→取消、阳光表单缺项校验、我的→诊断→关闭、清除账号确认→取消。源为基线加测试巡检接线，生产 UI 未改变。
- 系统 macOS 26.6.2，窗口内容 800×600，窗口截屏含标题栏为 800×632，截图 API 输出为窗口逻辑分辨率，物理 DPR 未直接核实；深色随系统。不能由该尺寸声称手机/平板验收。
- 截图 `docs/design/evidence/ui-ux/p0-*.png`：login/home/schedule-rich/exam/grades/classroom/spoc/judge/bykc/libbook/signin/cgyy/ygdk/ygdk-form/form-error/confirm/profile/diagnostics/account-confirm。均为合成数据，可提交。评教页已实际观察，前次图片仅在工具显示，文件未归档，不虚列图片。
- 前置问题与点击数见 `docs/design/ui-ux-audit.md`；所有84项静态去向见 `ui-ux-inventory.md`，双冻结操作合同见 `ui-ux-parity-review.md`。主要子视图的完整最终操作仍在P4–P6完成，当前只记已执行证据。

### 原生 iPhone 宿主回归

设备 iPhone 17 Pro，iOS 26.5，ID `B196C00E-FDA6-45C2-BAC5-D7208B8D9DBD`。命令从 `apps/ubaa_flutter` 执行：

```sh
CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=1 /Users/moorefoss/Dev/flutter-3.41.9/bin/flutter test integration_test/app_flow_test.dart -d B196C00E-FDA6-45C2-BAC5-D7208B8D9DBD
```

| 轮次 | 日志（本机 /tmp/ubaa-ui-ux-20260908/） | 结果与原因 |
|---|---|---|
| 初始 RED | p0-iphone-integration.log | 5通过/2失败；query.dart 对懒加载尚未构建卡片取 first/getRect，No element |
| 修复1复验 | p0-iphone-integration-green.log | 5通过/2失败；卡片标题与摘要同名，scrollUntilVisible 用文本匹配出现 Too many elements |
| 修复2复验 | p0-iphone-integration-green2.log | 7/7通过；以网格内唯一Card查找，真实滚动并断言命中，保留所有typed参数与写入断言 |

iOS Podfile.lock 的唯一依赖变化是既有 integration_test 开发插件被本次原生测试纳入 CocoaPods；没有升级 Flutter、FRB 或业务依赖。测试修复仅 query.dart；没有修改生产布局以适配测试。此7项通过是原生宿主fake交互证据，含十二入口、各领域typed查询、十种写操作及unknown/business-false分支，非真实FRB/上游或最终改版验收。

### P2 原型首次自评

原型 `docs/design/evidence/ui-ux/prototype.html` 自包含、合成数据。第一次手机390×844渲染发现三个摘要卡纵向占约510像素，快捷入口首屏不可读；登记 PROTO-01，先调整为课程主摘要+作业/预约并列紧凑摘要，再复验。尚不将原型标通过，更不计入最终App设备证据。

## P0 提交前门禁与审查

`just refs`、`just layout-check`、`just contract-version-check`、`just check-sensitive`、`just check` 均退出0；普通check的ShellCheck明确SKIP，最终check-strict仍待安装固定0.11.0后执行。宿主analyze先报15个巡检if缺大括号，纯格式修正后analyze退出0。iPhone最终原生宿主7/7退出0。所有结果为本轮，不继承历史计数。

独立审查认为默认生产隔离与懒加载查询测试修复符合规格。巡检InspectionBackend仍有R1参数/固定写目标不匹配、R2大量数据分页信封不一致、R3批量/提交后过滤使用固定结果三项问题，必须在用它做P4–P6完整操作验收前修复。已有Inspection截图仅证明入口/布局与具体已观察合成状态；iPhone7/7使用原_IntegrationBackend/_AllWritesIntegrationBackend测试注册，不能借这些结果宣称Inspection全部参数语义正确。主代理接受此证据限制，不降低最终验收要求。

## P2 完整设计与原型自评

- 规格：`docs/design/ui-ux-redesign.md`；实施清单：`docs/design/ui-ux-implementation.md`；84项完整去向和真实接口消费/产出已核对。
- 本地浏览器渲染 `http://127.0.0.1:8765/prototype.html`，外部评审视口1440×1180，内部iframe精确为手机390×844、平板768×1024、桌面1280×800；100%文字分别浅/深两主题，各9场景共54张。PNG仅裁剪掉设计工具栏，没有改动原型内部内容。
- 九场景为home/login/grades/library/bykc/form/confirm/profile/error，命名 `prototype-{场景}-{phone|tablet|desktop}-{light|dark}.png`。backend=HTML合成设计原型；日期2026-09-08；对应功能AUTH/SET/NAV/SCH/GRD/LIB/BY/W-CG/W-COM。源码是本轮未提交HTML，内容摘要见同目录manifest；非Flutter或原生设备证据。
- 已实际查看所有54张的六张评审拼图，主字段无非预期重叠，长表单与超出首屏内容用原型自身滚动。PROTO-01修复后手机至少两行完整快捷入口，范围内视觉问题已用于调整设计。
- 390×844/深色/130%文字操作场馆表单“继续确认”→正确场馆确认摘要→“确认提交（仅原型）”→结果待核对提示，未发生业务请求。原生软键盘与真实生命周期留P3–P6。
- P0基线已提交 `5fb00f33`。设计自评支持进入生产实现，完整目标仍未完成；P1部分子视图最终视觉随领域批次补齐，不复制原型为App证据。

## P3 状态连续性与巡检合同修复（实施中）

- 新增 `packages/ubaa_ui/test/navigation_continuity_test.dart`，原四项在基线全部RED：断点后搜索变空、返回重入view变summary、未应用学期/周次丢失。日志 `p3-navigation-red.log`。生产修复仅shell稳定挂载已访问领域，并捕获各自feature、账户username变化清上下文。
- 聚焦5项GREEN（包括账户清理额外RED/GREEN），旧widgets/goldens 91项通过；日志 `p3-navigation-green.log`、`p3-account-red.log`、`p3-widgets-green.log`。这些是widget证据，不推断原生窗口验证。
- Inspection三项审查问题补6项契约RED（两个动态目标、分页、批量顺序、签到与评教提交过滤），修复后6GREEN；审查又指出场馆用户页码误+1，新增第7项先观察expected1/actual2，再按生产映射统一一基页码，最终7/7GREEN。日志 `inspection-contract-red.log`、`inspection-page-red.log`、`inspection-contract-green2.log`。对应宿主analyze通过。
- 独立审查两范围规格/质量均无剩余已证实阻断；保留最多十二个访问页面的内存代价，跨loading/empty等切换导致内部列表重建仍属于后续状态场景。
- macOS已从当前源码重新构建并启动显式fake宿主，`p3-macos-inspection.log`；随后CUA明确返回设备锁屏，无法进行窗口缩放/输入观察。标记 `BLOCKED(environment_locked)`，不是认证失败，不绕过锁屏。
- 正在补原生iPhone/iPad integration截图导出入口：使用锁定SDK原生takeScreenshot及实际操作，记录真实view尺寸；不把tester改视口作为设备验收。最终macOS人工复验保持未完成，继续独立代码/门禁任务。

### P3 原生移动宿主逐页证据

2026-09-08，显式合成backend、原生iOS 26.5，未设置tester.view.physicalSize。iPhone17Pro实际402×874/DPR3，iPadPro11(M5)实际834×1210/DPR2。每设备两组主题场景（亮度为测试绑定注入，非修改系统设置）均退出0，各36张原生截图已查看，包含登录/首页、12领域默认视图、周表草稿返回重入、个人页/诊断/返回首页。断言实际点击和输入保留，截图由SDK的iOS原生插件导出；不是golden或桌面改窗口尺寸。

```sh
# apps/ubaa_flutter；每轮使用全新私有绝对输出目录，以下DEVICE取实际iPhone/iPad ID
UBAA_UI_EVIDENCE_DIR=/tmp/ubaa-ui-ux-20260908/本轮设备目录 CARGO_INCREMENTAL=0 CARGO_BUILD_JOBS=1 /Users/moorefoss/Dev/flutter-3.41.9/bin/flutter drive -d DEVICE --driver=test_driver/ui_render.dart --target=integration_test/ui_render_test.dart --dart-define=UBAA_UI_SOURCE_SHA=e93d569e+P3-uncommitted
```

原始日志 `p3-iphone-render-r5.log`、`p3-ipad-render.log`。仓库证据在 `docs/design/evidence/ui-ux/p3-native/{iphone,ipad}/`，逐图有步骤/源码标签/系统/原生尺寸/主题/backend/时间JSON和PNG哈希。源码标签明确为e93d569e加本批未提交实现，不冒充最终固定SHA验收。两设备12张评审拼图位于本轮/tmp目录，用于逐张视觉检查；原始72张已归档。

接线失败记录：最初对返回void的hide使用await导致编译失败；修复后登录按钮更新缺pump导致未登录；其后native binding不注册TestTextInput，改为结束真实焦点。均为测试接线问题，无生产逻辑绕过，原失败日志r1–r4保留本机。最终r5与iPad分别完整通过，未通过旧产物冒充新运行。

视觉结论：状态连续性修复在原生手机/平板可见；尚存原始周次/楼层/节次标签截断、手机首页单列卡偏大、平板详情大块空白，属于UX-02/05及后续设计系统迁移。本批没有改视觉或更新golden，不将这些缺陷称已解决。macOS人工缩放复验仍environment_locked；P6最终三设备、横屏/分屏/键盘/放大字与全部状态未完成。

ShellCheck固定0.11.0现已安装，`just check-strict`退出0，包含普通check集合，消除了此前ShellCheck SKIP；本批refs/layout/sensitive/contract也通过。下一批导航3项RED、主题2项RED已观察，尚未实现，不将这些未来用例计入本批GREEN。

## P3/P4-A 任务导航与共享视觉（复验中）

新增学习7项/校园5项目录，保留原 ordinary/advanced 公开列表；首页十二项全可达。断点600/1000、正文最大1200、Material3蓝色种子与明暗层级、16/12圆角和运行时主题已实现。Host主题仅内存，导航/重建不会新增查询；重启恢复系统主题。

行为证据：新增导航3项、主题2项及既有状态连续性5项通过；Host22项、UI非golden101项通过，3组golden尚待主代理逐项检查更新。analyze无问题。初次今日入口测试因测试手势在顶部触发真实下拉刷新而失败，改为仅重置滚动位置，保留onRefresh fail及12入口断言。独立审查未发现已确认阻断问题，仍要求实际视觉验收。

工具链补充：官方与OHOS锁定Flutter再次校验通过。Android SDK有29/35/36平台、emulator/adb，但无AVD/system-image，也未发现sdkmanager；尚无Android原生运行证据。实际DevEco26.0.0.821/API26存在，后续OHOS构建复用该路径。

本批原生复验：iPhone与iPad各2主题场景、38PNG，源码标签 `b2b91d46+P3A-visual-r2`，实际尺寸仍402×874/DPR3、834×1210/DPR2。新增个人页实际选择相反主题→恢复系统的断言与截图。手机首轮38图发现首页空白偏多，收紧156后再次完整跑通；33张非首页图像素完全相同，其余5张逐图重检。平板38图逐图检查；截图原件/元数据/哈希在 `docs/design/evidence/ui-ux/p3a-native/`。已知查询标签截断与领域平铺留在UX-02/05/06对应后续批次。

横屏尝试命令追加 `--dart-define=UBAA_UI_LANDSCAPE=true`，iPad两主题均因真实尺寸1668×2420未旋转而失败，driver退出1（p3a-ipad-landscape.log）；没有覆盖tester.view，也没有把失败标成宽屏PASS。macOS CUA本次再次报告锁屏。宽屏当前只有已检查golden，原生宽布局验收仍缺。

视觉基线：先查看13张领域实际差异图并对照原生文字，更新具体文件；再把响应式12张注册为独立testWidgets，保留600断点/异常/详情断言，使首张像素失败不再阻止其他场景生成。随后逐图检查主页与12张响应式实际图后更新。最终UI115项全部通过（测试注册拆分增加11项，不是新增11种业务）；Domain25通过，Host22通过，UI/Host analyze通过，just check-sensitive与just check退出0。OHOS只读check-toolchain退出0，0失败0警告；尚非HAP构建证据。

新导航下原iPhone宿主集成7项再次全部通过，退出0（p3a-iphone-integration.log），覆盖typed查询和十项合成写操作；不是新增真实写授权或P5三端全部场景完成。

## P3 补充：刷新上下文和短视口（RED 已观察）

`packages/ubaa_ui/test/refresh_continuity_test.dart` 六项当前1通过/5失败，生产容器尚未修改。最终RED日志 `p3b-refresh-red5.log`：success→loading→success、success→loading→stale、empty→success三项本地搜索实际变为空；844×390+键盘占位120的侧栏溢出86像素；360×800、文字1.3、键盘占位320且有实际路线Chip的图书馆查询溢出56像素。相同竖向占位下场馆查询通过。键盘占位是widget布局证据，不是原生键盘实测；早期fixture缺resolvedRoute隐藏了路线占位，补成实际App常见结构后才观察到图书馆溢出。

定位：FeatureDetailView按状态替换详情列表子树，丢失列表State；外层Column无法在短可用高度内容纳查询/路线/详情/返回；NavigationRail未开启已有scrollable能力。后续修复保留列表State并使短视口内容/侧栏可滚动，不放宽查询/资格或业务断言；对应原生复验必须随修复补齐。

### P3B 已实现与复验范围

三个生产文件已修复：详情列表常驻稳定子树，隐藏时ExcludeFocus/TickerMode防止旧控件键盘交互；明确empty仍传空details并清掉失效评教选择。短可用高度的详情整页可滚动，640×文字比例为受测组合的最小内容高度，正常竖屏golden不变；侧栏启用锁定SDK既有scrollable。empty再有结果只宣称保留搜索，不宣称恢复空结果前的旧滚动。

原生旋转r6/r7发现首次就地切深色后课程标题/副标题消失。增加确定性颜色测试后确认显式Text.style保留旧浅色颜色（0xFF1A1B21），而当前主题为0xFFE2E2E9。不是延长截图等待可以解决的问题。detail_list外层build现在显式依赖Theme，保证列表委托更新样式；新测试与其余6项聚焦共7项GREEN，根目录UI全量122通过，26张golden未再更新。UI analyze通过；check-sensitive/check退出0，Rust33个测试输出组累计988通过（含doc-test分组，Shell合同另计）。独立审查未发现本批阻断问题。

原生旋转最终重放r9的原失败平台亮度路径：XCTest1项+Flutter1场景，双进程退出0；两主题10张原图由主代理逐图检查，首深色标题颜色恢复，新增原生样式等于当前Theme的断言通过。实际834×1210↔1210×834/DPR2，无view覆盖；目录 `evidence/ui-ux/p3b-native/rotation/`，修复前原图在同级before-fix。真实个人页切主题r8另有通过证据，仅说明那条路径正常，不能取代r9的原路径闭环。

旋转接线：新增独立UI Testing target/scheme，只由XCUIDevice.orientation发原生方向事件，不重装/启动生产App；Dart只在真实metrics/草稿/截图完成后确认。XCTest无法读取本机Flutter辅助树，故测试仅用127.0.0.1:48763+每轮唯一runId交换阶段，不含业务数据，端口占用即失败，结束关闭服务。Foundation引用修为SDKROOT路径，最终原生target build9退出0；生产Info.plist/分屏配置均未修改。

手机P3B追加四场景：首轮真实键盘inset=0，2失败/2刷新场景通过，driver退出1。确认本机Simulator二进制存在ConnectHardwareKeyboard选项后，以 `open -g -a /Applications/Xcode.app/Contents/Developer/Applications/Simulator.app --args -ConnectHardwareKeyboard NO` 启动（原无Simulator前端进程，仅启动参数，不写持久偏好）。r2四场景driver退出0，但检查后发现键盘metadata仅60/70逻辑像素动画早期占位，Flutter窗口截图不包含系统键盘；这轮只作为中间证据，不用于关闭完整软键盘验收。正在补稳定占位、按钮位于键盘上方及整台模拟器原图。刷新失败后搜索/旧结果的原生明暗场景已观察通过。

### P3B 稳定键盘最终复验与阶段门禁

r3源码候选为 `581556b9+P3B-uncommitted`，iPhone17Pro/iOS26.5实际402×874/DPR3，四业务场景通过且driver退出0（日志 `p3b-iphone-keyboard-r3.log`，`+6`包含setup/teardown）。两主题键盘均稳定335逻辑像素，各8个连续样本，稳定820/816ms、观察1053/1049ms，应用按钮bottom166≤键盘top539。1.3字体与平台亮度为明确测试覆盖，view尺寸/DPR/inset未覆盖。

`docs/design/evidence/ui-ux/p3b-native/keyboard/` 保存8张Flutter窗口图以及2张simctl整机图，主代理全部检查；只有后两张包含真实系统键盘。查询按钮上方可见，应用后typed area/segment/day保真，未预约；成绩刷新失败保留“程序设计”搜索和旧结果。私有loopback48764只交换唯一runId与scene，截图成功才ACK后继续，测试结束关闭。专用driver调用原ui_render入口，未改变生产入口或全局driver。独立审查限定范围内无必须修项；命令UDID与截图必须一起核对，不仅依赖环境变量名称。

复跑命令从 `apps/ubaa_flutter`：设置新的绝对空目录 `UBAA_UI_EVIDENCE_DIR`、新的 `UBAA_KEYBOARD_RUN_ID`、实际 `UBAA_UI_SIMULATOR_ID`，运行锁定Flutter的 `drive --target=integration_test/ui_query_keyboard_test.dart --driver=test_driver/ui_keyboard.dart -d <同一设备ID> --dart-define=UBAA_UI_SOURCE_SHA=<候选> --dart-define=UBAA_KEYBOARD_RUN_ID=<同一新UUID>`。原生旋转用独立RunnerRotationUITests scheme先build-for-testing，复制同目录xctestrun设置EnvironmentVariables.UBAA_ROTATION_RUN_ID，再以同UDID串接test-without-building与ui_rotation_test.dart/ui_render.dart；两进程必须退出0，且metadata真实横竖尺寸互换。

P3B提交前refs/layout/check-sensitive/check与UI122已通过，App analyze发现两条仅语法风格提示后修正并复验；未改动已跑原生测试的行为。P3共享框架的窄/中/宽由原生iPhone竖屏及iPad实际横竖覆盖，不能替代P6手机横屏/平板分屏/macOS人工窗口等最终矩阵。

## P4 开始时的 Core-live 只读检查

2026-09-08约03:43–03:45（Asia/Shanghai），Core未改的 `c4984e96` 基线；此前refs/check-sensitive/check通过。依既有安全脚本串行执行 `RUST_LOG=off just verify-live mode=direct`、`RUST_LOG=off just verify-live mode=webvpn`，两命令均退出0。凭据只经忽略文件→stdin，自动临时会话目录随进程清理；不保存stderr，不发真实学校业务写。

Direct共40安全记录均PASS（末条elapsed_ms=38388）；WebVPN共38PASS+2NOT_APPLICABLE（末条18603），Judge current结果0，因此detail/details_batch为no_assignment_id，不计为实际详情读取通过。Direct与WebVPN的Judge include_expired数量分别83/17、current分别48/0，为本轮路线观察差异，保留事实，不据此改变解析或声称两路线数据等价。两路线场馆用途均 `source=static_fallback`，不当作上游用途已读证据。图书馆area_detail/seats本轮均成功，虽在03时非手册建议窗口，按实际安全结果记录，不凭时间猜失败；以后失败仍需营业窗口复测。

原始安全行位于本机 `/tmp/ubaa-ui-ux-20260908/p4-baseline-live-{direct,webvpn}.safe.log`；仅逐操作状态/计数/原因，未含业务正文或个人字段。该轮证明当前Core-live路径可运行，不取代P6最终源码候选或生产App只读。

## P4-B 学业领域与只读返回栈（实施中）

在 `c4984e96` 后新增domain typed学业展示与只读导航，不改Bridge/Core。App六项缺失投影RED后characterization17通过、domain28通过；周课表两个分支共用映射，考试独立起止时间/成绩字符串与null/教室完整节次保留。UI初次5项均RED后领域卡片/按天课表/宽成绩表GREEN；审查发现宽表漏课程编号与长标题固定行高，已补编号副行和自适应行高，不推算GPA或解析时间排序。

父子导航前先修读取归属：默认loadFeature与显式summary区分，FeatureReadContext含不可变query与requestRevision；跨query失败清旧数据、同query失败保持stale。独立readCacheEpoch仅使UI父帧缓存失效，不改阳光回读失败保留原快照的合同。三项App RED后受测读取/写生命周期/回读96通过，随后App全量199通过；domain30通过，App/domain/Host analyze通过。

UI用稳定帧保存父query/结果/草稿/搜索/滚动，返回不重查；typed term/week点击后仅查一次。初始3项导航RED后GREEN，独立审查再发现epoch通知复活旧子页及延迟权威回读被忽略，追加2项RED后以新requestRevision边界修复，两项与既有回归通过。首次UI全量129通过1失败（默认null query使服务端分页隐藏），恢复列表层默认query用于分页后widgets102+导航5通过。最新额外加入“刷新当前查询”真实按钮，用已应用query或默认读取，不采用未应用草稿；对应先RED。

Inspection新增四学业typed结构并保留many/long模型，两个新增fixture合同先RED后与旧7项共9通过；没有把更新合成字段当作实时数据。原P3键盘stale入口改为点击真正的当前默认读取刷新，避免把默认读取→不同参数应用冒称同query刷新。原生学业全部子视图/状态入口正在编写，尚无本批原生PASS或阶段提交。

P4-B门禁补充：`just check`退出0，refs/layout/check-sensitive通过。首次 `just flutter-check` 中Domain30、Platform48、App199、UI134、Bindings15通过；Host有1项对象identity旧断言失败，因为App现在冻结批量键后传递独立query。该测试改为完整hasSameParameters并保留领域/次数/view/week断言，非放宽业务参数；Host22复验通过，最后Flutter宿主包另行继续。

新增刷新图标造成19张既有golden各270像素变化，差异严格仅为右上角20×19区域；主代理两批查看全部新图并核对像素边界后复制对应19张，其他golden未动，随后widgets102通过。此处旧golden仍使用通用fixture，不作为新学业typed结构的唯一视觉证据；新领域视觉由原生巡检补齐。

原生iPhone r1：14业务case，10PASS/4FAIL，82图由验收代理逐张检查；4失败均为测试helper误把文本输入框的内部横滚当宽表，normal/long仅前段完成，不能计全功能通过。主代理复核周表、1.3长课名、成绩等原图，确认并修复UX-02/03及stale缺旧数据来源说明。学业短标签+按视图显示必填，保留原验证；新增两个行为RED后聚焦26通过。helper改为只找显式horizontal SingleChildScrollView，仍核验真实滚动位置与typed查询。r2源码标签改为 `c4984e96+P4B-native-fixes-r2`，使用新空目录。

最后官方Flutter宿主widget测试还发现P3A漏迁移“主页”旧文案断言；仅更新为已实施的“今日”，保留实际登录与课表入口断言，宿主包12项复验通过。当前全工作区134+2新UI用例与其余包仍需汇总最终门禁，不把首次失败入口写成退出0。

P4B第二次完整 `just flutter-check` 退出0：Domain30、Platform48、App199、UI136、Bindings15、Host22、官方Flutter宿主12，共462项，格式/analyze全部通过；日志 `p4-flutter-check2.log`。标签调整涉及10张golden，仅查询字段范围差异，逐图查看并核对区域后更新；两批golden更新均有精确列表/比较图保留在本机临时证据目录，未执行 blanket update。

### B1 原生复验闭环与继续范围

iPhone r2、iPad r1均为 `c4984e96+P4B-native-fixes-r2`，14业务场景各自全部通过，两个driver退出0（`+15`含tearDownAll）。手机实际1206×2622/402×874/DPR3，平板1668×2420/834×1210/DPR2，未覆盖view；字体1.0与long场景1.3为明确测试输入。手机106、平板112原图由验收代理全部逐图查看；主代理复核正常周表/条件教室/stale/长成绩与考试、平板周表与宽成绩。平板额外6张来自两主题周表/long周表/long成绩的真实横移。

原图、小型metadata和SHA256清单位于 `docs/design/evidence/ui-ux/p4b-native/{iphone,ipad}/`；不含review拼图、真实学校数据或系统凭据。metadata的readContext明确是App最新backend读取，并非返回父页缓存：父返回截图同时断言搜索/草稿恢复且requestRevision未增加，不冒称发生新父查询。全部查询路径使用inspection backend，无真实写入。完整命令为锁定Flutter `drive --target=integration_test/ui_academic_test.dart --driver=test_driver/ui_render.dart -d <实际UUID>`，环境 `UBAA_UI_EVIDENCE_DIR`指向新绝对空目录并传上述source define；每轮日志在本机p4b-iphone-academic-r2.log / p4b-ipad-academic-r1.log。

设计复核确认B1不是整个P4-B：共享学期选择器、教室日期/typed楼层节次选择、考试/教室宽布局及成绩待出分组留B2。iPad1.3日期框末位遮挡作为B2修复输入，不把可编辑横滚当作理想字段可读性；宽表横移后关键课程上下文也需在B2改善。EXM-01设计表中“时间排序”改为保持Core顺序，与公开时间字符串格式未保证可解析的事实一致，不猜时间或把空值填今天。macOS仍environment_locked，已在继续独立工作时异步请求方便时解锁；尚未得到确认，未绕过锁屏。


## P4-B2 实施与门禁反馈（原生验收进行中）

候选基于83ee9e89未提交变更。共享学期加载、typed教室选项/日期、明确今日与兼容查询、分组宽表/楼层导航已经实现。AppController学期生命周期/并发回归后App全208通过；UI新增选项、异域模型fallback、宽表本地展开/长文本与教室导航回归。审查发现epoch变化同时新请求loading使选项success被忽略，新增预期失败后修复；教室10+学期日期7+导航6共23通过，独立审查复核无阻断。A→B→A草稿期间迟到请求若最终参数相同且App generation/epoch仍有效，可以接受，并非需要人为禁止的旧结果。

第一轮flutter-check退出1：8张golden变化及1项旧课表测试假设今日显示学期/周次。旧参数测试改为明确选择“按输入查询”，保留term/week并追加summary断言；8图逐图检查，差异为今日控件收敛、手机结果上移、教室日期宽度及按钮（像素区域分别有本机记录），只复制对应8张。第二轮Domain30/Platform48/App208/UI157/Bindings15通过，Host因新SDK本地化传递依赖锁文件尚未刷新而退出65；继续刷新Host锁文件与复验，不记整门禁PASS。just refs、just check退出0。宽表实际宽度测试初次错误地在已有Shell的helper再次扣侧栏，已纠正为真实iPad834输入；没有以错误测试驱动降低生产740断点。新增1.3主要列边界及本地详情回归通过。

日志保存于本机/tmp/ubaa-ui-ux-20260908/p4b2-*.log，含失败记录；失败golden材料移出仓库至p4b2-reviewed-golden-feedback。此时尚未运行B2手机/平板原生轮次，不继承B1的106/112张图片作为本批PASS；macOS仍待解锁后完成实际交互。


B2门禁终态：第三轮`just flutter-check`退出0，七包依次30/48/208/157/15/22/12，总492。Host与OHOS锁文件仅补同一SDK本地化和intl0.20.2，未更新既有依赖；`just layout-check`退出0且无baseline违例，`just check-sensitive`退出0（1739文件）。周卡独立星期上下文先RED再10项学业展示回归GREEN。生产候选冻结`83ee9e89+P4B2-r1`，新增原生测试已format/analyze通过，iPhone开始；此时尚无本轮原生PASS。


B2手机r2终态：退出0，14业务场景全部通过（日志+15含teardown），110张PNG/逐图JSON。全部经37张检查页逐图审查，root另看暗色日期picker与明色summary兼容周表原图；中文、星期上下文和查询参数完整。已将检查过的原图/metadata/manifest及SHA256SUMS存入`docs/design/evidence/ui-ux/p4b2-native/iphone/`。仍发现共享设计待办UX-13：手机查询条件尚无折叠按钮，已在P3补项并排入P4-A；不修改正在运行的B2生产候选。iPad原生仍进行中，手机未补专门日期输入框视角，旧iPad1.3输入遮挡将由平板专图验证。


B2平板终态：iPad r1退出0，14业务场景全部通过（+15含teardown），122张原始PNG/JSON与manifest逐项一致，1668×2420/DPR2。全部经41张检查页逐图复核；root另看明色1.3日期输入专图、暗色正常成绩与暗色长考试原图。主要宽表列同屏可读、课程点击本地详情不发请求、楼层导航不发请求；日期末位完整，内部横滚偏移与最大范围均0。已归档至`docs/design/evidence/ui-ux/p4b2-native/ipad/`并生成SHA256SUMS。两设备共232张最终通过原图，仅synthetic-inspection；主题/文字倍率是明确测试平台输入，原生viewport未改。

原生命令沿B1入口：工作目录apps/ubaa_flutter，`flutter drive --target=integration_test/ui_academic_test.dart --driver=test_driver/ui_render.dart -d <设备UUID> --dart-define=UBAA_UI_SOURCE_SHA=83ee9e89+P4B2-r1`，UBAA_UI_EVIDENCE_DIR分别为本机p4b2-iphone-academic-r2与p4b2-ipad-academic-r1绝对新目录。iPhone r1退出1（12通过/2正常流程测试步骤错误，80图），完整失败边界保留在本机报告，不覆盖或标成功。最终App analyze无问题，format 3文件0改动。P4-B领域批次已闭合并阶段提交；P3共享折叠补项/P4-A/C/D/P5–P7继续，macOS锁屏及最终真实只读/三端/构建门禁未完成。


## P4-A 资料、设置及共享筛选补项（进行中）

B2阶段提交`0f3803cab121325534c37735d974fc6072cc0c82`后工作区clean，refs再次通过。先资料来源记录与设计一致性裁决，再Domain/App字段RED与映射RED；App20项characterization、Domain31通过，analyze无问题。UI个人页本地展开/联系人遮罩、关闭/切账号清旧状态与手机筛选折叠已实现，独立review无阻断；追加切Tab和查询持焦折叠验证。资料初次两项UI行为RED后GREEN，测试自身SemanticsHandle需在try/finally结束前dispose；扩展跨Tab测试需完整12快照，未把夹具缺失异常修进生产。

另一个真实本地生命周期缺陷：清除确认打开后同Shell换账号，旧回调仍执行。新增测试观察expected0/actual1后，用原ProfileView context.mounted阻止过期确认；正常取消/确认保持。首轮全UI158通过3失败：2张预期手机golden变化，1旧lazy列表finder因新增资料入口高度而未构建清除按钮；已逐图更新两张，测试真实滚动查找并补返回按钮实际命中断言。UI全量复跑中。A原生fixture/操作覆盖仍准备，不与B2原生证据混算。


A完整门禁：`just flutter-check`退出0，31/48/211/161/15/22/12=500；`just check`、layout退出0。敏感检查最初拒绝一个合成电话数字格式，改为全零后check-sensitive退出0，profile3项单独复验通过，不降低规则。原生测试部分聚合客户端先fixture RED/GREEN，真实BridgeBackend消费partial/direct ready+WebVPN failed的typed结果，未实现业务读明确失败；同时直接注入生产UnavailableBackend验证失败不进入演示成功。

A完整native phone r1、iPad r1均退出0，各15业务+1fixture通过（+17含teardown），33/32张原图已逐图复核。手机long邮箱因滚动拍摄角度出画面，不能以测试通过冒称完整可读，另6个profile场景补拍10图并复核；成功合成会话恢复另2主题/设备补充运行中。生产固定`0f3803ca+P4A-r1`，实际theme/textScale/themeMode写入逐图metadata，文件名的系统主题不替代用户切换后的真实主题。原生记录均为合成数据/内存保险箱，不是实际上游、Keychain或真实会话持久化证明。


A原生归档终态（代码固定0f3803ca+P4A-r1）：完整phone/iPad各15业务+1fixture均exit0；phone资料补充6业务/10图、phone与iPad成功恢复各2业务/2图全部exit0。五个独立目录、79张PNG/79逐图JSON全部审查，root另看手机折叠查询、partial失败主页、手机资料和iPad长资料原图，已归档`docs/design/evidence/ui-ux/p4a-native/{iphone/full,ipad/full,iphone/profile,iphone/restore,ipad/restore}`，各有独立manifest/SHA256SUMS。资料专项修正拍摄位置，不修改生产；旧完整矩阵手机masked/revealed图顶部因正常滚动裁切，不用来证明长邮箱，专项三行全文与iPad1.3全文图才是对应证据。

命令：apps/ubaa_flutter下锁定Flutter drive，target=integration_test/ui_account_test.dart、driver=test_driver/ui_render.dart、source define=0f3803ca+P4A-r1，phone/iPad UUID沿设备表，证据目录分别为本机p4a-iphone-account-r1、p4a-ipad-account-r1、p4a-iphone-profile-supplement-r1、p4a-iphone-restore-supplement-r1、p4a-ipad-restore-supplement-r1。资料专项追加UBAA_ACCOUNT_PROFILE_CAPTURE=true，恢复追加UBAA_ACCOUNT_RESTORE_CAPTURE=true。实际Theme/文字倍率/ThemeMode记录到metadata；均未修改原生view。恢复来自signedIn合成状态，Keychain/真实session/真正上游仍未据此通过；查询折叠证据为真实焦点与最终insets0，不冒称OS软键盘整机复验。宏观P3补项与P4-A本批已闭合，P4-C/D与P5–P7未完成。


## P4-C 课程作业、签到与评教（实现后，原生待验）

开始基线 `bb868cd212039a135041e76e7f0a39cadbc9df69`，分支 `codex/ui-ux-redesign`；`just refs` 退出0（本机 p4c-refs.log）。Domain/App 投影交接：Domain33、App223通过，analyze两层无问题。RED分别确认原缺失导航/父归属/overview/Signin presentation，随后追加同 query overview 的失败保留及新 query 清空；显式 failure→stale 已收窄瞬时三类，鉴权与权限仍清空，固定路线评教失败仍保留原失败规则。

根 UI RED：p4c-ui-red.log 4项缺入口/选择/总进度/嵌套搜索；p4c-ui-controls-red.log 与 p4c-ui-signin-red.log 分别记录配对选择、评教宽表、缺签到目标说明缺失。独立审查又发现混合未知模型移除合法作业入口，p4c-ui-fallback-red.log 后逐条 fallback 修复。最新12项新用例（10 content +2 navigation）通过，包含 B→A 顺序、includeExpired、返回父页无重读、搜索保留、弹窗epoch失效、大字号长题目。p4c-ui-regression-r2.log 110通过（当时8项content加102既有widgets）；全部新用例已分别重跑。UI analyze-r2无问题。

这些均为确定性测试；390/834视口模拟不作为原生设备证据。课程作业原生入口仍在构建，本批实际截图、交互复验及阶段提交尚未完成。既有SPOC选择器测试迁移到typed弹窗，并故意保留错误展示编号证明不按字段反推。全旧golden复验通过，本批无需更新旧图。


C门禁续记：UI全量173通过（p4c-ui-full-r3.log）；App按领域拆分旧测试后27项通过。layout初次发现features直属18文件及bridge_backend_test1001行超限，按课程作业子目录/领域测试拆分后0项违例通过，未新增baseline例外。敏感扫描当时2413文件通过。完整flutter-check首次停在并行编写中的新native测试尚未format，非生产运行失败；等待原生入口稳定后重跑。Core check因同一layout首轮失败，结构修复后的重跑仍待终态。macOS CUA复查仍明确锁屏，没有实际应用观察。


C续验：结构修复后的 `just check` 退出0。宽评教表在834逻辑宽、1.3字号下准备/确认/脱敏提交的原用例通过，明确使用canonical action目标，非原生证据。SIG-02按已审设计将筛选文案改为“可签到”，App计数摘要相应修改；UI/App两项均先观察原“未签到”缺目标/文案不符RED，参数signinPending与allowed过滤不变，原始signStatus=0仍展示“未签到”。


C原生首轮iPhone：`p4c-iphone-coursework-r1`，生产标签`bb868cd2+P4C-r1`；3项fixture通过，16项业务8通过/8失败，退出1，68图保留。6项失败因脚本误期望手动“应用筛选”不带既有隐藏08:00/22:00默认字段，已修改测试为完整精确参数比较（typed直接导航仍不带这两字段），未改生产请求。另2项是真实UX-16：第2页停在上一页滚动位置，首项不可见，已原图确认并独立RED后修复。宽评教确定性测试整套运行时受旧用例setSurfaceSize污染，补本用例显式设置/清理后widgets102通过。

当前候选`bb868cd2+P4C-r2`仅新增公共分页内容key修复，9项分页/父返回/导航连续性通过；iPhone全组r2运行中，iPad尚未启动。完整flutter-check与Core check随本次修复重新运行。TEST-C1已由fixture精确empty断言RED/GREEN修复，正常Signin ID与Core形态校准；first-error只允许一次重试并精确读取计数，不以条件二刷掩盖上下文问题。


C候选r2门禁：完整flutter-check退出0，七包33/48/223/174/15/22/12，共527项；Core check退出0。其后为诊断追加UI手机视口父返回1项和正式App控制器父返回1项，分别单测通过，尚未把它们并入新的全量计数。

iPhone r2退出1：3fixture与14/16业务通过，100图；两正常case停在单项返回后换搜索，分页两主题已通过。r1全部68原图已由独立审查覆盖，除UX16未见新增生产阻断；发现TEST-C2两张loading图与metadata不一致而排除，其余不因此自动升级最终候选证据。normal专项日志确认search-b步骤后controller仍judge-a，已要求逐点核对输入连接与unfocus时序，不用上滚或改controller绕过。加载辅助逻辑改Completer并新增契约后4项通过，正式原生重跑待诊断结束。


## 用户修正：以旧版界面为基础优化

用户明确要求减少冗杂，并以ubaa_old旧版UI为基准。新规格ui-ux-old-baseline.md已建立；只读确认三主入口/侧栏/56高顶部栏/功能子页隐藏手机底栏，just refs退出0。首版C专项输入诊断已越过旧失败点，证据支持测试输入/失焦时序问题；保存当前轮后暂停旧布局全量原生。此前C未阶段提交，投影与业务保护保留，视觉与布局将继续调整。此前图、计数、部分PASS均不升级为新规格已完成。


## O1/O2 新旧版基准续跑

2026-09-08，分支 codex/ui-ux-redesign；refs退出0，无遗留Flutter测试/构建进程。保留原未提交C投影和测试。`flutter test test/old_shell_baseline_test.dart` 首轮0通过/4预期失败（本机 `/tmp/ubaa-old-shell-red.log`）：四导航、手机底栏/重复返回、路线Chip、常驻查询与搜索均未满足新方向。开始修复共享外壳，旧候选截图不计本批PASS。


O1/O2首轮：5项旧版外壳测试与6项迁移后的返回栈测试共11通过。iPhone原生明暗两个遍历场景通过，56张合成图；iPad同场景退出0但已选中Rail标签点击有miss warning，不能作为该次点击证据，后续改为命中图标并加fatal hit-test复验。原生图复核确认默认十二领域均无常驻搜索/查询；同时发现打开的面板固定85%高度造成大量空白，用户也明确指出此问题。新增面板高度和研讨室名称两项均RED（本机ubaa-old-panel-size-red-2.log），修复内容自适应后13项通过（ubaa-old-panel-green.log）。首轮图片因此只作为问题与修复前证据，不当作最终候选。

用户于约09:10打开旧版iPhone App供观察。经CUA实际操作首页、普通菜单、SPOC列表与排序筛选弹窗（取消）、高级菜单、研讨室根页和预约时段选择页（未选择或下一步）、评教读取页。只记录布局，不保存真实课程/账号/预约/成绩原图到证据目录，不点击真实业务写入。旧运行包版本未核验，高级菜单含课程签到与更多占位，和当前冻结文件有差异，因此作为视觉观察而非冻结SHA证明。

观察结论：旧SPOC课程分组+紧凑卡片；筛选按需Dialog。旧研讨室先进入预约／我的预约／密码三卡，再选择校区、日期、楼层和时段表。当前仅调整外壳尚不满足这些完整层级，O3继续逐页继承。


### O1/O2阶段验证与遗留范围

- 课程作业/签到/评教Domain-App投影已单独提交 `277b3594`；Domain33、App223和check通过。UI及新外壳作为下一阶段提交，旧有效投影未清空。
- 旧版骨架、按需查询与实际路线修复后，UI完整182项通过（本机ubaa-old-ui-full-r8.log）。26张结构golden先在本机独立目录生成候选、逐张检查，与原图确认三导航/单顶栏/无常驻查询变化后才更新；此类golden仍用测试字体，只证明结构，不作为中文字体或原生证据。
- 手机iPhone17（62B0C851-ABFA-4169-B5B6-A2A155588931）与iPadPro11 M5（9F9D9C27-D085-467E-86B6-2960D0082ECC）各两个明暗遍历场景退出0、各56图，全部逐图检查。原生视口未覆写；每页检查无默认TextField、单顶栏、手机无底栏，成绩面板实际高度小于400并验证草稿关闭再开。截图索引：`docs/design/evidence/ui-ux/old-o2-native/{iphone,ipad}`；源候选标签`bb868cd2+O1-O2-r2`。iPad第二轮启用fatal hit-test，改为点击未选中的导航图标，没有第一轮重复点击隐藏标签的问题。为保留用户打开的旧版iPhone17Pro，本轮新App改用另一台iPhone17模拟器。
- macOS实际启动显式合成App，经CUA查看首页、普通功能、成绩页和按内容收缩面板。CUA中文输入/粘贴遇到既有ENV-01（中文未进控制器及剪贴板读取超时），未将该次手工输入记为草稿成功；原生integration与widget输入单列。
- 用户再次授权真实账号后，以现有verify脚本读取.env.local并经stdin登录；Core本轮Direct40PASS，WebVPN38PASS+2NOT_APPLICABLE，均exit0（约26秒）。两条Judge当前作业缺失的detail/batch N/A不算详情成功；Cgyy用途仍按source字段区分。
- 生产App使用独立Sandbox私有目录，安全Core登录生成的新会话由标准`lib/main.dart`恢复，未注入fake或改生产认证。CUA实际观察今日课表、待办、SPOC列表/只读详情和直连路线说明。新增`ui_live_readonly_test.dart`消费同一生产bootstrap，在macOS实际App遍历十二根页面：Direct首轮全部success/empty；WebVPN首轮SPOC为upstreamChanged、其余成功，保留失败。WebVPN第二次同会话完整启动恢复与遍历全部通过，SPOC恢复且没有触发显式刷新；Judge本轮为空，与首轮数量不同，只记实时观察，不据此改协议。测试只输出固定状态/路线/计数，不截取真实个人数据；不调用任何业务写入。
- 七包门禁前六包33/48/223/182/15/22通过；官方宿主迁移旧入口与索引后13项通过，合计536项。本机完整脚本因先前未使用import及宿主旧入口失败的记录保留；最终完整入口正在复跑，不把拆分终态伪写为该旧命令exit0。

本批只完成O1/O2共享骨架与现有查询能力收纳。O3逐页旧版卡片/子菜单、全部待办来源、P4-D、全部P5写入合成流程、最终旋转/分屏/多窗口/键盘、最终生产只读候选与P7尚未完成。真实App另发现SPOC分值被误标成绩；暗色侧栏账号头对比不足、首页卡片偏高及资料设置页多余刷新入口在下一批先修。不得以本轮112图或真实根页面遍历冒充全功能最终验收。


O1/O2最终门禁补记：`just flutter-check`（本机ubaa-old-o2-flutter-check-r4.log）退出0，33/48/223/182/15/22/13共536项。`just check`、layout、敏感扫描均已通过；最后两处宿主测试的旧入口/样式lint已修，不删业务断言。O1/O2候选阶段提交完成后继续O3，最终P6/P7保持未勾选。

### O3-A紧凑首页和作业卡片（续验中）

源码基于 `596a3f0a`。已观察 `coursework_content_test.dart` 的旧课程重复/分值标签RED；兼容详情字段又在 `bridge_backend_characterization/coursework.dart` 增加“分值不是得分”断言，`/tmp/ubaa-old-o3a-score-red.log` 预期失败后只修SPOC两处展示标签，GREEN日志 `...score-green.log`。未修改协议、请求参数或Judge真实得分字段。

`just flutter-check` `/tmp/ubaa-old-o3a-flutter-check-r1.log` 退出0：33/48/223/183/15/22/13，共537项；`just check` `/tmp/ubaa-old-o3a-check.log` 退出0。当前golden全部自然通过，没有更新图像来消除差异。

原生iPhone17正常子视图入口 `ui_coursework_test.dart`，明确CourseworkBackend，r1命令退出0（4fixture+4明暗正常/空待评场景+teardown）；36张 `/tmp/ubaa-old-o3a-iphone-r1` 原生PNG和36份元数据已逐图观察。测试通过三导航、右上查询面板、整卡详情、精确typed单项/有序批量、父查询草稿和选择、全部/待评进度、未知资格、准备后取消，不执行任何真实学校写入。r2继续完整状态矩阵。

macOS首次原生test在签到unknown-2后连接断开，日志 `...macos-r1.log` 标记did not complete；CUA同时观察到空黑窗口，未将构建成功或部分检查点算作渲染通过，原因尚未定位。已安排独立重跑且与CUA人工巡检分开。SDK3.41.9 integration_test只注册Android/iOS截图插件，macOS检查点不生成冒充原生截图；实际窗口另以显式合成inspection入口和CUA复核。

O3-A课程原生最终子矩阵：iPhone17 r2和iPadPro11 r1均退出0，4fixture+4正常子视图/空待评+12状态矩阵（各平台测试输出另有teardown）。每端128张原生图及JSON已归档 `docs/design/evidence/ui-ux/old-o3a-native/{iphone,ipad}` 并生成SHA256SUMS；手机92状态图与前述相同UI的36正常图、平板128图均逐场景复核。未发现本批作业卡截断或搜索面板常驻；初始loading图可见测试点击标记和动画起始点，属于采样时刻，不是最终宣传图。

macOS r2全20项退出0（`/tmp/ubaa-old-o3a-macos-r2.log`），未改view。之后独立flutter run显式CourseworkBackend，CUA实际执行普通入口→SPOC列表→右上面板输入b→完成→整卡详情→返回→重新打开面板，草稿b与父过滤仍在；设置选深色后侧栏账号头对比正常。4张系统原始窗口图归档 `old-o3a-native/macos`，包括列表、详情、父草稿、暗色侧栏，附操作说明和SHA256SUMS。r1中断不覆盖。此证据关闭O3-A具体实现的运行核验，不关闭仍未恢复的评教紧凑行、预约子菜单、全来源待办与P5/P6全产品验收。

生产只读补验：先前O2隔离会话启动后呈登录页，未猜测为密码错误；重新建立新的私有App沙箱配置，通过既有 `verify-live feature=auth mode=direct` 安全stdin使用.env.local账号，prepare/login/status三项PASS，退出0，日志 `...o3a-app-auth.safe.log`。正常 `lib/main.dart` 恢复后，CUA实际观察到今日课程以名称/时间/地点显示，待办位于其后；从待办进入SPOC，真实未提交作业的score明确显示“分值”，顶栏说明为当前页面直连。没有保存真实页面截图、账号、课程正文或个人数据到仓库；没有点击任何真实写入按钮。该项是O3-A真实首页/作业展示补验，不能代替最终双路线全子视图矩阵。

### O3-B评教课程行与错误卡（续验）

基于 `eaa40b15`，先运行 `/tmp/ubaa-old-o3b-red.log`：错误卡短文案占满600高视口、宽评教仍为DataTable两项预期失败。实现按内容收缩错误卡、各端统一课程行和只读详情，单门选择后“准备评教”、多门“准备批量评教”。聚焦22项GREEN，完整flutter-check首轮仅旧宽表断言失败；按真实新入口迁移该用例，保留假展示ID不改变真实提交target、prepare/commit次数等全部业务断言，第二轮退出0共538项（33/48/223/184/15/22/13），日志 `...o3b-flutter-check-r2.log`。`just check` 同批退出0。

课程原生入口新增只读详情弹窗与单门prepare/cancel，再执行两门批量prepare/cancel，保留target四元组和commitCalls=0。macOS全20项退出0；iPad全矩阵退出0并保存132张图，本批变化涉及的42个评教/首次错误场景已逐图复核。手机首轮21个输出检查全部通过，但导出时DriverError/Service connection disposed，命令失败、未保存PNG；保留 `...o3b-iphone-r1.log`，不当作验收成功。仅测试入口增加显式主题过滤后按明暗两次导出，deep UI/业务与原生view不变：深色r2退出0，66张图成功，其中21个变化场景已看；浅色r2进行中。

macOS独立inspection以显式CourseworkBackend的first-error场景运行。CUA实际打开高级功能/教学评教，错误卡已收缩；重试恢复紧凑课程行；选一门→准备评教→确认→取消，再全选，显示2个合法目标，未知资格未被加入。未执行真实写入。窗口原图使用系统截图技能保存到临时目录，待同批归档；这与单独macOS原生断言证据区分。

O3-B最终补记：手机浅色r2退出0并保存66张，明暗合计132张；平板132张，两端共264张原图+元数据+校验和归档 `docs/design/evidence/ui-ux/old-o3b-native`。本批变化的两端各42个评教/错误场景均已逐图检查，未变课程页继续由完整矩阵回归；不是对未改页面另作像素设计裁决。macOS3张窗口原图也已归档。手机首轮仅在导出失败，分主题两次完整覆盖后导出成功；未掩盖原命令失败。所有本批prepare/取消均用显式脱敏backend，真实写入为0。


### O3-C子菜单、当前页标题与研讨室称谓

基于 `69fad426`。菜单/草稿/路线/迟到结果5项先RED；默认子页误发读取、首次错误自动重试分别补RED后修复，13项聚焦GREEN。三个根菜单golden新候选逐图检查后只更新对应三张；Ahem字体图仅检查结构，不代表原生文字。完整UI首轮10失败主要因测试辅助未进入新增子页，修正后仅三张旧golden不匹配；完整Flutter门禁r1和文案/测试目录迁移后的r2均退出0，33/48/223/191/15/22/13共545项。`just check`首轮触发UI测试目录17文件上限，将新增菜单测试归入navigation子目录后r2退出0；没有增加门禁豁免。

原生入口 `ui_old_baseline_test.dart` 增加三个根菜单全部8个子入口、子页当前标题/实际路线、返回菜单路线未确定、默认子页搜索draft关闭重开检查。显式 `UBAA_UI_LANDING_ONLY=true` 只缩小本批范围，默认仍检查十二根页；不改原生view。iPhone17与iPadPro11 r1均退出0，手机42张逐图观察发现合成数据仍写体育馆/羽毛球；改为合成研讨楼/研讨室/小组研讨后两端r2均退出0，各42张原图和JSON归档 `docs/design/evidence/ui-ux/old-o3c-native` 并生成SHA256SUMS。手机r2四张改文案图已复核，其余同布局在r1已看；平板r2全部42图逐场景检查。框架测试触点十字标记是采样原图的一部分，未美化移除。子页通用长卡与自然选择尚待P4-D，不因菜单通过而标为完成。

macOS原生入口明暗两项断言退出0（`/tmp/ubaa-old-o3c-macos-r1.log`），日志记录foreground失败。之后启动独立显式InspectionBackend准备CUA操作，computer返回“Mac is locked and automatic unlock could not unlock it”；本批macOS实际窗口视觉/手动操作证据尚未取得，已请求解锁并继续独立任务，不能把原生断言/构建替代实窗验收。所有本批业务数据明确合成，没有读取.env.local或触发真实写入；之前真实只读证据继续按各自候选记账。


### O3-D1图书馆单页选择（实施与复验中）

基于ba9fadde。投影缺失5RED后6聚焦GREEN；首次楼馆选择行为RED后接单页楼馆/楼层/分区/四列座位。宿主结合真实AppController的回归发现：重查楼馆丢原选中、隐藏页刷新多发两次子查询、分区查询空日期回落今天、兼容字段假ID覆盖typed picker、长ID造成42px横溢出、状态搜索缺失。分别保留 `/tmp/ubaa-old-d1-*-red.log` 和修复日志；最新宿主7项全部通过（`...host-r6.log`）。明确空结果清除选择的初轮测试误找刷新tooltip，修正后通过；不把该辅助失败说成已经观察到的生产缺陷。

手机原生r1命令失败4项：两正常流程fixture默认page=0导致RangeError、两首次错误用例误找FilledButton；34张图已逐场景检查。fixture补一基默认页RED/GREEN，按钮定位改实际TextButton。r2状态矩阵通过，但两正常流程在从底部摘要滚回楼馆时找不到懒加载控件；辅助滚动还选中了不可滚动的嵌套网格。r3按正确外层滚动并补状态搜索操作继续复验。新布局先按原图补10卡片间距、长芯片省略和面板字段宽度；不伪造截图通过。

`just check`本批r1退出0；Flutter门禁r1因TickerMode.of弃用lint失败，按固定SDK现有valuesOf(context).enabled修改后r2复跑。Core/Bridge认证协议、公开v9、写资格未变。macOS仍待解锁后的独立实窗核验，P4-D/P5–P7总目标保持执行中。


D1本批终态补记：手机r4、平板r1均退出0，1项fixture契约加14项明暗UI/状态场景完成（iOS另有teardown输出计数），每端46张图及JSON已逐场景复核并归档 `docs/design/evidence/ui-ux/old-d1-native`。正常流程含状态搜索、关闭重开草稿、allowed/denied/unknown座位、canonical预约准备/取消、换馆清旧选择、预约详情/取消准备、服务器第二页；真实写入为0。r2滚回父控件、r3清空搜索后的失败分别修正外层懒加载滚动与原生输入的实际点击/泵帧，沿之前C批次可靠输入方式断言controller文本后再关闭。修复后原路径通过，没有改产品业务逻辑掩盖输入工具时序。

macOS r1有相同输入/滚动定位及stale说明未滚入视口的4失败；r2修正辅助后15项通过、退出0（`/tmp/ubaa-old-d1-macos-r2.log`）。这仍是原生断言证据，独立CUA窗口被锁屏阻塞的限制未解除，不存黑图冒充实窗。最终生产App仍需解锁后按新候选补验。

完整 `just flutter-check` r4退出0：33/48/228/192/15/22/20共558项。r2的旧字段索引测试误把馆ID填入新增日期输入框，改为按字段标签定位；另旧失败重试用例原来点击被面板遮住的按钮仍凭旧applied值通过，现先关闭面板并断言真正产生第二次同参数查询。没有删除查询或资格断言。r3只有新测试的样式lint，按固定SDK修正后通过。`just check` r1退出0。

用户真实账号只读补验：经既有verify脚本安全stdin读取.env.local，串行Direct/WebVPN两轮均退出0；每轮auth prepare/login/status和libbook libraries/areas/area_detail/seats/bookings共8PASS。本次仅记录库3、区2、详情1、座位175、记录2的公开计数和路线，不保存原始响应或个人数据；日志 `/tmp/ubaa-old-d1-libbook-{direct,webvpn}.safe.log`。此证据是Core-live，不能冒充当前生产App的UI操作。

图书馆旧版静态分区地图仍需补迁移并验收；日期-时段关系没有因UI实现而扩展。O3/P4-D与P5–P7总目标未完成。


### O3-D1b静态地图与时段草稿回填

基于`ee7d08a5`。逐段核对冻结`LibBookReserveScreen.kt:205–244/343–415/486–539`的地图入口、29分区白名单和1–5倍缩放/重置。只从冻结资源导出画面：原29张JPG带作者等元数据，按原方向和ICC转换为sRGB，再从纯RGB像素生成无EXIF/XMP/作者等信息的PNG。`docs/design/libbook-map-sources.json`记录固定SHA、每张来源/资产/像素校验和与尺寸。冻结目录未修改；原生包验证29张均完整可读。静态地图明确说明颜色不是实时可用性，不能在图上直接预约；未知typed分区不借上一张或兼容字段中的地图ID。

地图入口/未知ID两项RED记录`/tmp/ubaa-old-d1b-map-red.log`，聚焦GREEN同前缀`map-green.log`。时段回填RED记录`...slot-red.log`，GREEN为`...slot-green.log`：点击时段仅回填公开原始id/start/end，日期仍为空；不发读取，空日期应用仍被既有校验阻止。完整手填路径保留。未扩展日期关联、Bridge或写资格。

`just flutter-check` r1退出0，33/48/228/194/15/22/21共561项；`just check` r1退出0。手机与平板原生r1均退出0，各58张PNG及JSON已全部逐场景核对，归档`docs/design/evidence/ui-ux/old-d1b-native`并附SHA256SUMS。包含地图适应/放大/拖动/重置/关闭再开、未知分区、三字段草稿及明确日期查询，以及D1全正常/空/首次错误/stale/加载/长文1.3/大量数据矩阵。所有操作使用显式LibraryBackend，真实写入为0。

macOS r1退出1：两项stale辅助滚动误选了SelectableText内部20像素滚动区；地图及其余场景通过。只修测试定位为可命中的大视口滚动区，不放宽命中断言，不改生产逻辑；r2全18项退出0。iOS原生图来自相同生产代码的r1；辅助修复只解决macOS小窗口中该失败路径。独立macOS实窗和生产App图书馆新UI仍受已记录锁屏条件阻塞，不能拿断言替代视觉验收。确认阶段左上drawer问题UX-O8仍在P5修复范围，不因为本批图片看过而关闭。

本批补齐LIB-03地图子能力，LIB-04支持三字段一次回填；仍保持84编号。下一步继续O3-D研讨室、博雅、阳光展示及全来源待办，然后P5–P7。


### O3-D2研讨室实施与首轮复验

基于`50147317`。旧版校区/日期/楼栋芯片→房间时段表→下一步、订单卡和门锁页已逐源码核对，九列协议复用和公开字段上限见`source-parity-ui-reservations.md`D2补记。五项展示RED（全部slot/无slot房间、重复timeId、站点用途、订单、锁）观察预期失败后GREEN；新增日期上下文RED补齐首站点查询草稿的原始日期。站点电话继续沿既有展示敏感保护不投影，不因公开结构含该字段扩大展示。

UI初轮测试两次因测试构造参数不匹配未编译；修正实际签名后r3两项行为RED，接自然选择与表格后GREEN。矩阵固定房间列，全部公开slot可见，仅独立合法且与当前日期/房间唯一匹配的action可选；沿既有timeOrdinal选择同房间相邻两时段，选中后才出现下一步。原始表单校验/prepare流程保留，改为共享收集函数供自然选择入口复用；订单保留typed详情与原取消目标。

原生phone/macOS r1正常流程在返回按钮处失败：测试用错tooltip名称；其余状态运行通过。手机32图已全部看过，发现选中格Text自带主题颜色覆盖按钮前景色，明暗对比不足，以及表单字段紧贴；按图修复颜色继承/间距并把表单摘要改为地点和时间。站点首次查询日期原先由面板默认值和fake固定日各自提供，现投影应用读取日期并自然传递，保留响应日期独立语义，不修改学校协议。r2仅修正确返回tooltip，三端中的phone/macOS已通过；视觉修正后的r3与平板继续执行，不沿用r2为最终候选图。

新增宿主四项回归通过：隐藏后首页刷新不扩展日期查询、换日期清旧选择/不增加返回层级、明确空结果不复活选择、typed站点picker不读假兼容ID。完整Flutter r1因extension调用protected setState警告停止，移回State实例方法；r2到宿主仅有两条新增测试fixture样式lint，修正后r3重跑。macOS独立实窗仍待解锁。P4-D其余领域及P5–P7未完成。


D2本批终态：`just flutter-check` r5退出0，33/48/233/196/15/22/25共572项；`just check` r1退出0。手机r4、平板r2全部14项明暗正常/状态原生场景退出0（iOS另有teardown计数），每端44张PNG+JSON归档`old-d2-native`。手机r1的32图、视觉修复后r3的42图、平板r1的44图均全部逐场景看过；最终手机r4/平板r2新改订单、门锁、用途的每端10个场景再次核对。订单标题不再因状态分配半宽提前换行，按钮已对齐；已选文字按按钮前景色显示，表单间距与地点/时间摘要已复验。

macOS r5全部14项原生断言退出0。之后独立`flutter run --target=integration_test/ui_rooms_test.dart --dart-define=UBAA_UI_INSPECTION=true`使用显式RoomBackend；CUA确认先前锁屏阻塞已经解除。实际坐标点击进入研讨室菜单/时段表、选择两个相邻时段、搜索输入1→关闭→重开、下一步表单→滚动→取消、我的预约→详情→准备取消→取消返回、门锁状态。六张系统原始窗口图已检查并归档`old-d2-native/macos`，按截图记录当时主题；系统主题在巡检期间由浅切深，未造成选择/返回丢失。没有执行commit或真实学校写入。

真实账号Core-live：通过既有.env.local安全stdin路径，Direct/WebVPN均退出0；每轮3auth+6研讨室读取PASS，站点7、用途10（source=static_fallback）、订单15、门锁可用信息0。日志`/tmp/ubaa-old-d2-cgyy-{direct,webvpn}.safe.log`只保留固定状态/路线/计数；未保存原始响应或个人数据。此项仍与生产App新UI分开，生产只读新页面以及之前O3-C/D1独立实窗现在可以继续补验，不因锁屏解除自动记为已验。

本批自然查询、原始动作和局部三端验证可提交；CG-02表单用途选择/完整写入闭环、P5异常和生命周期、全来源待办及博雅/阳光与最终P6/P7继续执行，未关闭总目标。


### O3-C/D1 macOS 独立实窗补验

基于`376d941a`。先停止本任务已结束的D2 inspection，独立运行`app_flow_test.dart`显式InspectionBackend；CUA使用合成登录进入普通/高级功能，逐个进入博雅3子入口和图书馆2子入口并返回，研讨室根菜单与其D2已完成子路径配套核验。三张根菜单原始窗口图归档`old-o3c-native/macos-backfill`。该旧fixture仅用于菜单/标题/返回，不拿其通用详情充当D1 typed业务结构。

随后独立运行`ui_library_test.dart`，仅测试inspection支持显式`UBAA_UI_STATE=map`；生产main不识别该参数。CUA实际操作LibraryBackend：地图放大/拖动/重置/关闭，下午时段原始三字段回填且日期为空，手填日期关闭重开仍保留，应用查询后选择A1，滚至准备预约→canonical目标确认→取消返回，回菜单进入我的预约→只读低频字段→canonical取消准备→取消返回。7张系统原始窗口图归档`old-d1b-native/macos-backfill`；主窗口800×632，内容800×600，深色。已逐张观察，另附operations和独立SHA256SUMS，不重写历史批次证据校验文件。确认页左上drawer已知UX-O8仍待P5；未执行commit，真实写入0。宿主analyze日志`/tmp/ubaa-old-macos-backfill-host-analyze.log`退出0、No issues found。

这补齐O3-C/D1/D1b之前因锁屏缺少的独立实窗；不是最终生产账号验收，不自动关闭其他页面或P5–P7。

本次补验提交前`just refs`、`just check`、`just check-sensitive`及暂存diff检查均退出0；check包含layout门禁。只新增明确合成窗口图，不保存真实账号画面。


### O3-D2b 真实只读发现的订单零基分页修复（执行中）

生产macOS普通main、隔离配置、安全stdin认证，基于ab1a777b。CUA仅过滤输出固定标题/状态/计数，不保存真实画面或原始AX。直连图书馆馆/层/区/时段可读，明确日期2026-09-08后只读座位结果，已渲染语义中空闲29、未知71（懒加载视口计数，不宣称全175），两条预约记录；图书馆分区、座位、记录路线说明均为当前页面直连。研讨室今日84格不可预约，切到09-09后14可预约/70不可预约，未选择无下一步、无常驻输入框，实际路线直连。

随后发现研讨室“我的预约”空列表，而Core-live同账号page=0为15条。定位普通App读取错误地把0改1；冻结API/ViewModel默认0，Local精确透传，Core/Bridge与取消回读也均0。完整来源九列复核追加`source-parity-ui-reservations.md`D2b。此前合成backend采用错误一基，D2原验收未覆盖该偏差；保留历史记录。本轮先增加App RED，观察实际发送1而期望0（`/tmp/ubaa-old-d2b-app-red.log`）；UI最初静态shell辅助在进入新查询后不能提供readContext，pumpAndSettle超时，该用例迁到真实AppController宿主，保留`...ui-red.log`为辅助失败。宿主RED观察输入第2页发2而非原始1（`...host-red.log`）。

修复普通读取保留零基、订单分页number+1作为通用一基显示；仅研讨室UI输入/前后页转换回原始page，恢复草稿反向转换。Core、Bridge、公开合同及取消回读page0不变。合成RoomBackend也改为零基数据切片、一基显示，并给原生正常流程增加原始page与前后页/草稿断言。App characterization35项与宿主5项GREEN；完整门禁、原生和生产两路线复验继续。


D2b原生与门禁补记：完整Flutter r1退出0，33/48/234/196/15/22/26共574项；just check r1退出0。iPhone/iPad r1各14项场景通过（另teardown输出），每端44张原图和JSON归档old-d2b-native。本次影响的明暗首页/第二页4图每端已复核；其余布局未改，完整操作回归通过。macOS r1全部14项原生断言退出0。

生产正常main修复后直连订单首页显示第1/1页共15条，详情可开、路线直连，门锁公开available=false且路线直连；WebVPN首页同样15条，详情和门锁实际路线WebVPN。所有记录只保留固定语义、计数，不打印个人字段，不执行prepare/commit。手工改分页时CUA报告的AX字段值与Flutter实际画面不一致，实际仍1/20，不能判定分页查询改变；短暂私有系统截图仅裁出无个人字段的搜索面板核对，不归档真实画面。重新绑定/置前/键盘/AX设置未取得可靠修改，保留此工具操作限制，不把它算产品分页PASS。改由新增显式生产只读原生入口完成真实分页输入和前后页验证；该入口只使用正常bootstrap与隔离配置，日志只输出状态/路线/页码/计数。首轮analyze发现query可空访问，修正后r2退出0，原生生产复验待执行。


D2b生产原生终态：WebVPN r1在旧隔离会话启动后240秒未进入主界面，退出1且未执行业务操作；增加只输出是否登录页/错误枚举的早期诊断，不改变生产认证。为每条路线新建私有目录并经既有stdin授权账号认证后，WebVPN r2和Direct r1均退出0，各1项约35秒，5个读取检查点：原始page0显示1/20条容量、首批15条；输入显示页2/容量5请求page1并返回5条；上一页原始0/显示1、下一页原始1/显示2；typed详情1条。每次实际结果route分别webvpn/direct，面板草稿2保留，businessWrites=0。结构化安全记录与生产补丁哈希归档`old-d2b-native/production-readonly.json`。旧会话为何未恢复尚无足够证据，不修改认证协议或扩大重试来掩盖失败。

CUA图书馆WebVPN：楼馆/层/区出现；点击首分区后的独立观察期内仍没有详情结果，路线明确未确定，没有把配置策略当实际路线，也没有记分区/座位/预约记录全通过。此项及P6实际物理键盘输入保留后续核验；输入定位生成的三张完整真实窗口临时图已精确清理，仅保留无个人内容面板裁剪，未触及只读会话或真实响应。D2b分页缺陷本身已由双路线生产原生读及三端合成回归闭合。

D2b提交前最终补记：新增生产只读测试入口后宿主analyze r3、just check r2、敏感检查r2均退出0。生产业务代码仍与574项门禁及三端原生候选一致；新测试入口未进入普通生产启动或默认确定性测试。84项功能编号未增删。


### O3-D3博雅实施（2026-09-09）

基于aa40d2e2。source-parity-ui-bykc.md逐页旧依据及公开字段上限已记录。投影5项RED在`/tmp/ubaa-old-d3-app-red.log`，6项聚焦GREEN（含原typed写能力），总次数独立0/未知、分类达标不重算、记录ID与课程ID不同。UI首轮静态辅助无法回送新readContext而两非默认子页超时；辅助补StatefulBuilder回送当前query/revision后r2观察3项行为RED。课程卡/统计/已选状态3GREEN，状态默认/草稿和typed picker另2RED后5GREEN。首轮picker误用Map而现有helper需要List，保留编译失败，改为原typed ID列表。

已选初版弹窗不满足旧独立详情页面，追加AppBar/顶部返回RED后改私有read frame；字段只来自当前已选记录，不新造网络API。新增宿主取消回同详情路径PASS，刷新为空后返回复活旧记录RED（`...host-red.log`）；现正补同步同查询祖先显示快照并复验。Flutter全量r1在picker类型处失败，r2在本地详情改动前已通过，不能拿r2覆盖后续read frame修改；最新本批门禁及原生仍待执行。


D3 macOS输入定位：显式BoyaBackend窗口普通搜索首次输入1确实显示；模拟super+a替换数字后继续无效。巡检日志明确出现Flutter HardwareKeyboard断言：KeyUpEvent物理Key A对应的logicalKey与此前KeyDown不同。此前为探索做的macOS数字键盘提示改为text实验已撤回，不为不合法模拟键事件修改产品输入/校验。只重启本次合成inspection清除按键状态，后续避免该组合键；不把这一记录扩大成真实物理键盘必然故障。原始失败日志保留`/tmp/ubaa-old-d3-macos-inspection.log`。

D3 r2补记：已选本地详情宿主2GREEN，UI连续滚动/旧面板/typed目标等6GREEN，之后新增选课显示时间RED（缺开始/截止标签）并修为旧规则，UI7GREEN。原生r1手机96图逐图检查发现教师/地点长文撑高列表、固定56宽标签在1.3字体拆字；r2列表单行省略，详情完整换行与标签自然宽度。手机r2受影响明暗长列表和详情24图复查、平板r2全部96图复查，问题修复。统计总次数零/null和分类权威保持，确认页UX-O8仍属P5。

Flutter r5退出0：Domain33/Platform48/App239/UI203/Bindings15/Host22/Flutter宿主28，共588项；`just check`r2退出0。日志前缀`/tmp/ubaa-old-d3-`：`flutter-check-r5.log`、`check-r2.log`、`ui-green-r2.log`。两端`flutter drive`r2各38场景（另teardown），iPhone402×874/DPR3、iPad834×1210/DPR2，iOS26.5，两主题、1.3长文、42条、empty/first-error/retry/stale/loading；每端96PNG+JSON。macOS`flutter test integration_test/ui_boya_test.dart -d macos`r2为38场景退出0，日志`macos-r2.log`。

macOS独立BoyaBackend实际窗口800×632、内容800×600、深色：三击选数字再键入2，面板关闭重开实际画面保留；课程整卡到详情/选课准备取消；已选到私有详情/滚动到底/签退准备取消回同位置；统计三列与独立总次数。7张系统原图和操作记录归档`docs/design/evidence/ui-ux/old-d3-native`，共199PNG、源码文件SHA256清单与证据校验清单。合成backend仅prepare/cancel；其简写签到摘要不含方向和时间窗，而生产prepare.rs本来带有，已记录P5补忠实fixture，未把缺省合成摘要当生产协议缺陷。

真实直连Core-live博雅五操作PASS（courses6、chosen0，均无个人字段）；生产原生r1在已进入主界面后等待summary读取240秒超时，退出1。定位测试误将默认loadFeature的null query要求为显式summary；`FeatureReadContext`合同明确区分两者，旧版根入口会复用默认快照。仅修测试summary接受有context且query=null，其他显式视图仍精确匹配，生产缓存、协议和资格未改。新生产入口首次analyze的nullable query错误已修；本批生产双路线复验尚在继续。

D3本批终态：Direct生产r2约32秒、WebVPN生产r1约20秒，各1场景退出0。五读取分别课程列表6条/显示第1页、typed课程详情1条、已选empty0条、统计header1条、白名单资料1条；实际route逐项匹配Direct/WebVPN，详情ID与点击课程相同，正文无常驻查询框。当前学期没有真实已选记录，因此真实已选详情未执行，不用合成结果冒充；合成已选详情已在三端覆盖。Core-live两路线分别认证3项+博雅读5项PASS；真实业务写入0。仅10条安全状态记录归档production-readonly.json，未保存真实截图/字段。新测试入口analyze r2、just check r3、敏感final均退出0；阶段提交后继续阳光/Home/P5–P7，不宣告总目标完成。

### O3-D4阳光实施中（基点e812798e）

已逐段阅读YgdkHomeScreen、ViewModel、ClockinFormScreen、API/DTO/Local与BackendTest；examples无等价协议，实施前对应表见source-parity-ui-ygdk.md。投影3RED固定零/null概要、typed项目与记录、普通和pinned投影一致；GREEN6（含原3项）。组合读取2RED后实现默认后台只概览、显式首页再固定路线records；局部非认证错误保留本次概要与稳定code，认证失效仍向外传播。首次测试误用BridgeErrorCode字符串及webVpn拼写编译失败保留home-red.log，修辅助后home-red-r2.log两条行为失败；app-green-r3.log完整45 characterization通过。原全部视图精确请求序列仅增加已授权的显式阳光首页固定记录请求，其他预期不删。

UI3RED后接旧概要→记录同页/新增项目选择/低频记录详情；完整跨天结束日期保留、图片仅计数。ui-green-r1为pattern条件组合语法编译失败，r2两通过、首条因测试缺完整照片/回读/提交能力而无新增入口失败；测试补齐显式能力（所有写回调抛错，未降低生产能力门槛）后r3三项通过。flutter-check-r1全包初轮通过，但后续宿主接线尚在改动，不能当最终D4结果。本批尚无新原生或真实读取验收。

D4复验续记：写后固定回读宿主r3观察更新记录缺失的预期RED，传同requestRevision记录快照后green-r1三项通过；此前r1误用fake input字段、r2缺明确照片权限能力均为测试辅助错误，没有放宽生产校验。低频字段进入详情后的搜索RED见filter-red，修共享搜索值后UI green-r4四项通过。最终`flutter-check-r3.log`退出0，Domain33/Platform48/App244/UI207/Bindings15/Host22/Flutter宿主31共600项。

手机与平板r1分别全28业务场景（iOS另teardown）退出0，各60PNG；macOS r1全28原生退出0。正常链包括旧概要记录、完整详情、按需搜索/分页草稿、项目选择/禁用项、明确合成照片和时间prepare/cancel、分页前后；状态涵盖empty/首次错误重试/局部错误/stale/loading/1.3长文/42记录。最后只修低频字段搜索，无布局变化；r2三端各明暗两正常链退出0，手机平板各20新PNG，含metadata-filtered。两端r1和r2全部160图已逐场景看过；照片表单紧贴和独立页面问题已列UX-D4-03交P5。

macOS独立SportsBackend已CUA实际操作概要/记录详情、按需面板、新增项目选择、缺时间照片表单验证和取消返回，4张深色系统原图归档；主窗800×632、内容800×600。CUA中文输入时AX与实际画面不一致，只记录工具限制，不记中文筛选通过；原生Flutter输入路径另已验证。164张原图及原生元数据归档old-d4-native，分r1全矩阵和r2受影响正常链；没有把截图当生产真实响应或最终P6证据。

D4本批终态：Core-live Direct/WebVPN各3认证+2阳光读取PASS，生产macOS两路线分别r1约37秒/32秒退出0。旧首页概要11项目且首批20记录、只读本地详情无额外读取，独立记录显示第2页/每页5条→上页显示1，关闭重开草稿2保留，每次actualroute匹配预期。仅6条安全读取状态及计数归档production-readonly.json；真实业务写入0。两次运行器foreground open returned 1提示保留，不把该只读断言当独立可见窗口截图；独立实窗另有合成CUA4图。`just refs`、layout、check-sensitive、check最终均退出0，84编号与HEAD完全相同。独立表单/提醒与全来源Home、P5–P7继续，未宣告总目标完成。
