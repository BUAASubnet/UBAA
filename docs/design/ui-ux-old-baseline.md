# 基于旧版界面的多端优化

2026-09-08。用户最新指令：当前校园应用 UI 太冗杂，以 `ubaa_old` 旧版实现为基础，仅做必要优化并适配多端。本文件是当前视觉与交互的优先规格；`ui-ux-redesign.md` 中与之冲突的首版四导航、聚合摘要和常驻操作区设计已被替代。84项功能、Core/Bridge业务约束、脱敏写入验证及最终手机/平板/macOS验收要求继续有效。

## 旧版基准

冻结版本 `6e75e120a26b0eefb3ab4a6f8251d1230db4a62e`，仅只读核对，不在冻结目录构建或修改。

| 组成 | 旧版准确来源（相对 composeApp/src/commonMain/kotlin/cn/edu/ubaa） | 本轮继承 |
|---|---|---|
| 主导航 | `ui/common/components/BottomNavigation.kt` | 主页、普通功能、高级功能三个入口，保留熟悉的功能归类 |
| 顶部栏与层级 | `ui/common/components/AppTopBar.kt`；`ui/navigation/MainAppScreen.kt:719`、`:1052` | 单条56高标题栏；根页菜单，功能/子页返回；功能页隐藏手机底栏，没有第二个常驻返回区 |
| 个人入口 | `ui/common/components/Sidebar.kt` | 资料、设置由侧栏进入，主导航不增加“我的”页签；账号退出沿原确认与清理流程 |
| 首页 | `ui/screens/menu/HomeScreen.kt`、`HomeTodo.kt` | 今日课表与待办，按真实课程/任务展示；不堆叠三块笼统摘要和全部功能面板 |
| 功能菜单 | `ui/screens/menu/RegularFeaturesScreen.kt`、`AdvancedFeaturesScreen.kt` | 图标、功能名称、短说明组成的简洁网格；旧普通8项，旧高级为研讨室/阳光/评教及无动作占位。本轮保留当前已实现的课堂签到入口代替空占位，不删除业务功能；读取错误在内容页反馈 |
| 视觉 | `ui/theme/Theme.kt`、`Typography.kt` | Material 3浅深主题、柔和表面、16页边距与12项间距；保留原有风格语汇，不另造品牌装饰体系 |
| 评教 | `ui/screens/evaluation/EvaluationScreen.kt` | 简洁进度与课程/教师行，选中后才显示批量操作；沿当前Core资格与统一确认，不能照抄旧版直接提交或默认全选行为 |

## 页面规则

1. 每页只保留一个标题和一个返回入口。手机进入领域内容后隐藏全局底栏；平板与桌面保留用于跨功能切换的侧栏，内容页不再放重复返回按钮。
2. 根据用户进一步明确的想法，搜索、筛选、查询统一收纳到顶栏右上角的一个入口。手机打开底部面板，平板/桌面打开侧面板；搜索仅本地过滤，条件更改后显式应用才读取。手填ID、多字段兼容查询继续完整可达，关闭面板保留草稿、搜索、选择与列表位置；未使用时不占正文空间。
3. 根据用户进一步明确的想法，直连/WebVPN仅使用顶栏一个路线图标；图标与tooltip表示当前显示页面的实际路线，点击显示路线说明及切换选项。未知时显示未确定，不把默认策略或已认证路线冒充本次实际路线；用户设置和诊断中的完整信息仍保留。
4. 状态卡、进度、筛选属于内容滚动的一部分，不能与固定批量栏/分页一起把列表压成窄缝。手机评教按旧版紧凑课程行展示，批量动作仅在有选择时出现；每个可写操作仍进入同一个确认状态机。
5. 列表只展示决定当前任务的字段，其余字段在本地详情中可达。SPOC/希冀的课程归属、原始截止/提交状态/成绩仍保留；typed父子导航、二元键和有序批量查询不改。
6. 手机维持单列，功能网格至少两列；平板按实际宽度增加网格列数，内容可采用列表/详情并列；桌面加宽导航与内容上限，鼠标/键盘、长文和滚动可用。主要布局遵循旧版列表结构，不为宽屏把每页都改成大表格。
7. 新布局不恢复旧版未经当前合同允许的行为：真实学校写入仍禁止，确认/未知结果/未尝试/固定路线回读保留；联系人默认遮罩，图书馆日期与时段未证明的关联不自动绑定；旧版已存在但当前公开合同未接入的能力继续单独列明。

## 实施与验收顺序

- O1：恢复三入口、原功能归类、侧栏资料设置、功能页单顶部栏与隐藏手机底栏；检查旧版主题和功能网格。首页以今日课表/待办为主体，数据不足时明确状态，不能将用户选中的学期结果冒充今日课程。
- O2：收敛领域共用查询/搜索/路线/返回区域，优先复验手机评教仅30逻辑像素结果区的问题；资料与设置拆清职责。新UI仍使用已有typed投影、读取生命周期和写状态机。
- O3：按旧版领域页逐个调整学业、作业、签到、评教、图书馆、研讨室、博雅、阳光。尚未完成的P4-D继续实施，完成的A/B/C也按新视觉基准复查，不用之前截图冒充新布局通过。
- 每批先行为RED/GREEN，实际运行手机/平板并检查图片和操作；最终macOS、原生旋转/分屏、真实只读及完整门禁仍按goal.md执行。旧方案的测试与原生证据保留为历史和回归依据，不删除失败记录。

## 当前自评

导航、单顶部栏和手机子页隐藏底栏直接来自冻结实现；本轮优化集中在响应式布局、内容密度、可达性和状态连续性。原业务投影和资格保护可继续使用。当前首版四导航和常驻工具区不再符合用户最新要求，不能以其已有测试或原生图片作为最终设计验收。旧版首页待办所需数据和各领域的具体控件位置仍须逐条核对后实现；没有真实用户研究，不声称已获得大学生群体验证。


## 顶栏交互补充（用户再次明确）

左侧菜单/返回、中间标题、右侧单个路线图标与搜索筛选入口。正常内容区不重复路线文字、大查询卡、搜索框或返回按钮。已应用的过滤可以用一行简短结果说明呈现；不把未应用草稿说成当前结果条件。关闭面板时释放键盘焦点，账号或读取生命周期变化后旧面板不得把旧资料/选项回填新账号。

O1/O2共享骨架已实现并通过UI182项及手机/平板各56图原生复验；本轮生产macOS也已恢复真实会话并只读巡检。O3逐页结构及最终P5–P7仍待完成。首版UI527项、手机C-r2 14/16通过等仅属历史，不作为新结构最终验收。


## 第二次逐页复查（用户指出名称与筛选占屏）

- `cgyy/CgyyHomeScreen.kt` 的根页面为预约研讨室／我的预约／查看密码三卡。业务名称统一为“研讨室预约”，现Core内部Cgyy/场馆类型不改名；密码仅依当前合同显示可用状态，不能恢复旧版原始返回值。当前通用站点列表不是旧版根页面，O3需恢复子入口与流程。
- `spoc/SpocAssignmentsScreen.kt:81` 是课程分组列表，搜索通过浮动按钮打开小型AlertDialog（`:145`附近），没有常驻搜索区。新顶栏搜索入口符合用户要求；第一轮面板固定85%高度使少量条件仍占大半屏，这是待修UI问题，改为内容自适应加最大高度，完整条件过长时内部滚动。
- `evaluation/EvaluationScreen.kt:32` 仅在有选中待评课程时显示执行按钮；课程列表前只保留说明与进度。当前未选中仍显示批量栏与禁用提交按钮不符合要求，需去掉空选择操作区；资格和确认状态机保持不变。


## O3-A逐页对应（开始于596a3f0a）

| 页面 | 旧版依据 | 必要优化 | 当前实现及待修 |
|---|---|---|---|
| 首页课程卡 | HomeScreen.kt的TodayClassCard：名称、时间、地点；实机观察同结构 | 保持单列和可扫读密度，长文字自然换行 | 去掉当前多余课程编号副标题，按旧版紧凑卡显示；无today投影不冒充今日课程 |
| SPOC列表/详情 | SpocAssignmentsScreen.kt:196–245与SpocAssignmentDetailScreen.kt:117：score标分值，课程分组、标题与状态、教师/起止/分值 | 长标题与状态可换行，批量/返回保护保留；全部原字段在详情/更多信息可达 | 当前误标成绩、重复课程名称、独占一行状态和大详情按钮需收敛；请求与DTO不变 |
| 希冀列表/详情 | JudgeAssignmentsScreen.kt课程分组与卡片、JudgeAssignmentDetailScreen.kt父作业/题目 | 保留有序二元键批量查询，选择不触发详情读取 | 当前重复课程副标题和批量选择长文占屏，按旧卡层级压缩 |
| 侧栏 | Sidebar.kt使用柔和表面上的账号卡，资料/设置入口；真实运行已核对 | 配对表面与文字颜色，明暗可读 | 当前暗色UserAccountsDrawerHeader底色和文字配对错误，修复；设置页不保留首页刷新图标 |

此批沿既有逐操作C1–C9对照，未改URL/service、重定向、cookie、方法参数、编码、加密、DTO类型、缓存权威、错误码、退出语义或写资格。数据缺失保持未知；SPOC字段标签只按冻结源修正，不改变字段值。

### O3-B 子菜单与研讨室页面的源码核对（实施前）

2026-09-08重新完整阅读冻结 `CgyyHomeScreen.kt`、`LibBookHomeScreen.kt`、`BykcHomeScreen.kt`：根页是两列居中图标卡片，分别为预约研讨室/我的预约/查看密码、预约座位/我的预约、选择课程/我的课程/课程统计。`BykcFeatureCard` 最小高160、图标48、16内边距、12网格间距；不在根页展示查询表单。当前新版仍直接进入通用结果，是待修缺口，不能把O2的外壳截图视为子菜单保真通过。

完整阅读 `CgyyOrdersScreen.kt`：预约地点与状态同一首行，下一行日期时间，再显示主题和活动类型；查看详情/取消为紧凑操作，空/首次错误与旧数据分开。当前取消仍必须经过WriteCoordinator，不照抄旧版直接写入。只读详情的公开字段上限和状态映射见 `source-parity-ui-reservations.md`。

完整阅读 `CgyyLockCodeScreen.kt`：旧版展示密码状态、密码与预约信息，定时30秒刷新。当前公开合同仅提供available；本轮保留该能力上限，不展示原始返回、锁码、交易号或个人数据，不为贴近旧UI扩展认证/公开合同。菜单说明应是“查看门锁状态”，避免承诺不存在的密码正文能力；旧版按钮名称与受限结果的差异需在84项清单明确保留。

补充已读旧界面：`LibBookReserveScreen.kt` 全文件显示楼馆→楼层→分区→座位（每行4席）→预约摘要→确认的顺序；地图为按已知分区ID映射的冻结本地图片，可缩放1–5倍及重置，没有座位坐标API。日期时段关联仍须遵守现有公开DTO限制。`LibBookBookingsScreen.kt` 全文件显示地点/状态同首行、日期时间次行、座位和条件取消，不能把未知状态默认成已预约。

`BykcStatisticsScreen.kt` 全文件是总体净有效次数卡和“课程小类／通过与指标／达标情况”表；保持DTO的isQualified，不从通过数重新授予业务资格。`BykcCoursesScreen.kt` 已核对列表、完整筛选弹窗：状态/类别/校区多选交叉筛选，自动加载更多；其旧常驻FiltersBar按本次用户明确要求移至顶栏按需面板，不继承占据正文的大工具区。更改页码或增量策略前仍须保留当前服务器分页合同。

### O3-B评教行和错误提示收敛

冻结 `EvaluationScreen.kt:198` 的 `EvaluationCourseItem` 是选择框/完成图标、课程名、教师与状态同行的卡片。新版两端纵向大卡和宽屏DataTable改回同一课程行；当前合法typed目标仍由既有 `_evaluationSubmitTarget` 提供，单门评教通过选择一门后执行现有准备流程，多门同一入口，避免每行重复常驻提交按钮。课程详情图标仅显示已公开字段，不在旧快照弹窗中创建写入意图。已评和未知资格仍分别显示状态/原因，不根据展示文案推资格。

首次错误大红卡来自 `FriendlyErrorCard` 内部Column默认占满高度。按旧版错误说明+重试的紧凑结构收缩，安全错误编号与复制能力保留。新增失败回归观察到600高错误卡占满600视口，以及宽评教仍存在DataTable；日志 `/tmp/ubaa-old-o3b-red.log`。本批实现/新原生验收正在进行，不沿用O3-A图声称完成。

### O3-C子菜单与当前页标题

按已读冻结三个HomeScreen恢复有查询能力宿主的根菜单：博雅3项、图书馆2项、研讨室3项。点击子卡发出既有FeatureQuery并压入当前只读返回栈；顶栏标题来自实际可见帧（冻结 `MainAppScreen.kt:688–708` 的课程/作业详情与预约子页映射），返回保留根查询草稿。菜单未展示预载数据，所以路线图标显示未确定；子页以该页读取结果实际路线为准，迟到结果不能替换已返回菜单。现有无查询回调的独立只读组件继续呈现宿主提供的快照，避免造出不能操作的菜单；生产宿主一直提供查询能力。

门锁菜单采用“门锁状态”，保留旧版第三项位置并准确说明现有公开available能力；不承诺明文密码。研讨室顶层图标按 `AdvancedFeaturesScreen.kt:47` 使用DateRange，说明改为研讨室语义。根菜单的查询面板保留完整参数入口，本地结果搜索只在结果页显示；手动应用根查询同样进入子页，不覆盖菜单。5项菜单/草稿/实际路线/迟到响应测试已RED，日志 `/tmp/ubaa-old-o3c-menu-red.log`。

默认子页复用匹配的预载快照，含首次错误和加载状态；打开菜单不隐式重试，显式应用仍读取。缓存依据是冻结 `BykcViewModel.kt:95–111`、`LibBookViewModel.kt:63–83`、`CgyyViewModel.kt:85–100` 的ensure/initialLoadedOnce保护。对应新增重复读取及首次错误RED记录后，13项菜单/读取导航聚焦回归通过（`/tmp/ubaa-old-o3c-menu-green-r3.log`）。UI完整首轮因测试导航辅助未进入新增子页失败，修正辅助后的第二轮仅三张旧根页golden不匹配；三个新候选已逐图观察，正在原生验收，未声明P4-D完成。
