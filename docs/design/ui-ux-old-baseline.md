# 基于旧版界面的多端优化

2026-09-08。用户最新指令：当前校园应用 UI 太冗杂，以 `ubaa_old` 旧版实现为基础，仅做必要优化并适配多端。本文件是当前视觉与交互的优先规格；`ui-ux-redesign.md` 中与之冲突的首版四导航、聚合摘要和常驻操作区设计已被替代。84项功能、Core/Bridge业务约束、脱敏写入验证及最终手机/平板/macOS验收要求继续有效。

## 旧版基准

冻结版本 `6e75e120a26b0eefb3ab4a6f8251d1230db4a62e`，仅只读核对，不在冻结目录构建或修改。

| 组成 | 旧版准确来源（相对 composeApp/src/commonMain/kotlin/cn/edu/ubaa） | 本轮继承 |
|---|---|---|
| 主导航 | `ui/common/components/BottomNavigation.kt` | 主页、普通功能、高级功能三个入口，保留熟悉的功能归类 |
| 顶部栏与层级 | `ui/common/components/AppTopBar.kt`；`ui/navigation/MainAppScreen.kt:719`、`:1052` | 单条56高标题栏；根页菜单，功能/子页返回；功能页隐藏手机底栏，没有第二个常驻返回区 |
| 个人入口 | `ui/common/components/Sidebar.kt` | 资料、设置由侧栏进入，主导航不增加“我的”页签；账号退出沿原确认与清理流程 |
| 首页 | `ui/screens/menu/HomeScreen.kt`、`HomeTodo.kt` | 今日课表与待办，按真实课程/任务展示；不堆叠三块笼统摘要和全部功能面板 |
| 功能菜单 | `ui/screens/menu/RegularFeaturesScreen.kt`、`AdvancedFeaturesScreen.kt` | 图标、功能名称、短说明组成的简洁网格；旧普通8项，旧高级为研讨室/阳光/评教及无动作占位。本轮保留当前已实现的课堂签到入口代替空占位，不删除业务功能；读取错误在内容页反馈 |
| 视觉 | `ui/theme/Theme.kt`、`Typography.kt` | Material 3浅深主题、柔和表面、16页边距与12项间距；保留原有风格语汇，不另造品牌装饰体系 |
| 评教 | `ui/screens/evaluation/EvaluationScreen.kt` | 简洁进度与课程/教师行，选中后才显示批量操作；沿当前Core资格与统一确认，不能照抄旧版直接提交或默认全选行为 |

## 页面规则

1. 每页只保留一个标题和一个返回入口。手机进入领域内容后隐藏全局底栏；平板与桌面保留用于跨功能切换的侧栏，内容页不再放重复返回按钮。
2. 根据用户进一步明确的想法，搜索、筛选、查询统一收纳到顶栏右上角的一个入口。手机打开底部面板，平板/桌面打开侧面板；搜索仅本地过滤，条件更改后显式应用才读取。手填ID、多字段兼容查询继续完整可达，关闭面板保留草稿、搜索、选择与列表位置；未使用时不占正文空间。
3. 根据用户进一步明确的想法，直连/WebVPN仅使用顶栏一个路线图标；图标与tooltip表示当前显示页面的实际路线，点击显示路线说明及切换选项。未知时显示未确定，不把默认策略或已认证路线冒充本次实际路线；用户设置和诊断中的完整信息仍保留。
4. 状态卡、进度、筛选属于内容滚动的一部分，不能与固定批量栏/分页一起把列表压成窄缝。手机评教按旧版紧凑课程行展示，批量动作仅在有选择时出现；每个可写操作仍进入同一个确认状态机。
5. 列表只展示决定当前任务的字段，其余字段在本地详情中可达。SPOC/希冀的课程归属、原始截止/提交状态/成绩仍保留；typed父子导航、二元键和有序批量查询不改。
6. 手机维持单列，功能网格至少两列；平板按实际宽度增加网格列数，内容可采用列表/详情并列；桌面加宽导航与内容上限，鼠标/键盘、长文和滚动可用。主要布局遵循旧版列表结构，不为宽屏把每页都改成大表格。
7. 新布局不恢复旧版未经当前合同允许的行为：真实学校写入仍禁止，确认/未知结果/未尝试/固定路线回读保留；联系人默认遮罩，图书馆日期与时段未证明的关联不自动绑定；旧版已存在但当前公开合同未接入的能力继续单独列明。

## 实施与验收顺序

- O1：恢复三入口、原功能归类、侧栏资料设置、功能页单顶部栏与隐藏手机底栏；检查旧版主题和功能网格。首页以今日课表/待办为主体，数据不足时明确状态，不能将用户选中的学期结果冒充今日课程。
- O2：收敛领域共用查询/搜索/路线/返回区域，优先复验手机评教仅30逻辑像素结果区的问题；资料与设置拆清职责。新UI仍使用已有typed投影、读取生命周期和写状态机。
- O3：按旧版领域页逐个调整学业、作业、签到、评教、图书馆、研讨室、博雅、阳光。尚未完成的P4-D继续实施，完成的A/B/C也按新视觉基准复查，不用之前截图冒充新布局通过。
- 每批先行为RED/GREEN，实际运行手机/平板并检查图片和操作；最终macOS、原生旋转/分屏、真实只读及完整门禁仍按goal.md执行。旧方案的测试与原生证据保留为历史和回归依据，不删除失败记录。

## 当前自评

导航、单顶部栏和手机子页隐藏底栏直接来自冻结实现；本轮优化集中在响应式布局、内容密度、可达性和状态连续性。原业务投影和资格保护可继续使用。当前首版四导航和常驻工具区不再符合用户最新要求，不能以其已有测试或原生图片作为最终设计验收。旧版首页待办所需数据和各领域的具体控件位置仍须逐条核对后实现；没有真实用户研究，不声称已获得大学生群体验证。


## 顶栏交互补充（用户再次明确）

左侧菜单/返回、中间标题、右侧单个路线图标与搜索筛选入口。正常内容区不重复路线文字、大查询卡、搜索框或返回按钮。已应用的过滤可以用一行简短结果说明呈现；不把未应用草稿说成当前结果条件。关闭面板时释放键盘焦点，账号或读取生命周期变化后旧面板不得把旧资料/选项回填新账号。

O1/O2共享骨架已实现并通过UI182项及手机/平板各56图原生复验；本轮生产macOS也已恢复真实会话并只读巡检。O3逐页结构及最终P5–P7仍待完成。首版UI527项、手机C-r2 14/16通过等仅属历史，不作为新结构最终验收。


## 第二次逐页复查（用户指出名称与筛选占屏）

- `cgyy/CgyyHomeScreen.kt` 的根页面为预约研讨室／我的预约／查看密码三卡。业务名称统一为“研讨室预约”，现Core内部Cgyy/场馆类型不改名；密码仅依当前合同显示可用状态，不能恢复旧版原始返回值。当前通用站点列表不是旧版根页面，O3需恢复子入口与流程。
- `spoc/SpocAssignmentsScreen.kt:81` 是课程分组列表，搜索通过浮动按钮打开小型AlertDialog（`:145`附近），没有常驻搜索区。新顶栏搜索入口符合用户要求；第一轮面板固定85%高度使少量条件仍占大半屏，这是待修UI问题，改为内容自适应加最大高度，完整条件过长时内部滚动。
- `evaluation/EvaluationScreen.kt:32` 仅在有选中待评课程时显示执行按钮；课程列表前只保留说明与进度。当前未选中仍显示批量栏与禁用提交按钮不符合要求，需去掉空选择操作区；资格和确认状态机保持不变。


## O3-A逐页对应（开始于596a3f0a）

| 页面 | 旧版依据 | 必要优化 | 当前实现及待修 |
|---|---|---|---|
| 首页课程卡 | HomeScreen.kt的TodayClassCard：名称、时间、地点；实机观察同结构 | 保持单列和可扫读密度，长文字自然换行 | 去掉当前多余课程编号副标题，按旧版紧凑卡显示；无today投影不冒充今日课程 |
| SPOC列表/详情 | SpocAssignmentsScreen.kt:196–245与SpocAssignmentDetailScreen.kt:117：score标分值，课程分组、标题与状态、教师/起止/分值 | 长标题与状态可换行，批量/返回保护保留；全部原字段在详情/更多信息可达 | 当前误标成绩、重复课程名称、独占一行状态和大详情按钮需收敛；请求与DTO不变 |
| 希冀列表/详情 | JudgeAssignmentsScreen.kt课程分组与卡片、JudgeAssignmentDetailScreen.kt父作业/题目 | 保留有序二元键批量查询，选择不触发详情读取 | 当前重复课程副标题和批量选择长文占屏，按旧卡层级压缩 |
| 侧栏 | Sidebar.kt使用柔和表面上的账号卡，资料/设置入口；真实运行已核对 | 配对表面与文字颜色，明暗可读 | 当前暗色UserAccountsDrawerHeader底色和文字配对错误，修复；设置页不保留首页刷新图标 |

此批沿既有逐操作C1–C9对照，未改URL/service、重定向、cookie、方法参数、编码、加密、DTO类型、缓存权威、错误码、退出语义或写资格。数据缺失保持未知；SPOC字段标签只按冻结源修正，不改变字段值。

### O3-B 子菜单与研讨室页面的源码核对（实施前）

2026-09-08重新完整阅读冻结 `CgyyHomeScreen.kt`、`LibBookHomeScreen.kt`、`BykcHomeScreen.kt`：根页是两列居中图标卡片，分别为预约研讨室/我的预约/查看密码、预约座位/我的预约、选择课程/我的课程/课程统计。`BykcFeatureCard` 最小高160、图标48、16内边距、12网格间距；不在根页展示查询表单。当前新版仍直接进入通用结果，是待修缺口，不能把O2的外壳截图视为子菜单保真通过。

完整阅读 `CgyyOrdersScreen.kt`：预约地点与状态同一首行，下一行日期时间，再显示主题和活动类型；查看详情/取消为紧凑操作，空/首次错误与旧数据分开。当前取消仍必须经过WriteCoordinator，不照抄旧版直接写入。只读详情的公开字段上限和状态映射见 `source-parity-ui-reservations.md`。

完整阅读 `CgyyLockCodeScreen.kt`：旧版展示密码状态、密码与预约信息，定时30秒刷新。当前公开合同仅提供available；本轮保留该能力上限，不展示原始返回、锁码、交易号或个人数据，不为贴近旧UI扩展认证/公开合同。菜单说明应是“查看门锁状态”，避免承诺不存在的密码正文能力；旧版按钮名称与受限结果的差异需在84项清单明确保留。

补充已读旧界面：`LibBookReserveScreen.kt` 全文件显示楼馆→楼层→分区→座位（每行4席）→预约摘要→确认的顺序；地图为按已知分区ID映射的冻结本地图片，可缩放1–5倍及重置，没有座位坐标API。日期时段关联仍须遵守现有公开DTO限制。`LibBookBookingsScreen.kt` 全文件显示地点/状态同首行、日期时间次行、座位和条件取消，不能把未知状态默认成已预约。

`BykcStatisticsScreen.kt` 全文件是总体净有效次数卡和“课程小类／通过与指标／达标情况”表；保持DTO的isQualified，不从通过数重新授予业务资格。`BykcCoursesScreen.kt` 已核对列表、完整筛选弹窗：状态/类别/校区多选交叉筛选，自动加载更多；其旧常驻FiltersBar按本次用户明确要求移至顶栏按需面板，不继承占据正文的大工具区。更改页码或增量策略前仍须保留当前服务器分页合同。

### O3-B评教行和错误提示收敛

冻结 `EvaluationScreen.kt:198` 的 `EvaluationCourseItem` 是选择框/完成图标、课程名、教师与状态同行的卡片。新版两端纵向大卡和宽屏DataTable改回同一课程行；当前合法typed目标仍由既有 `_evaluationSubmitTarget` 提供，单门评教通过选择一门后执行现有准备流程，多门同一入口，避免每行重复常驻提交按钮。课程详情图标仅显示已公开字段，不在旧快照弹窗中创建写入意图。已评和未知资格仍分别显示状态/原因，不根据展示文案推资格。

首次错误大红卡来自 `FriendlyErrorCard` 内部Column默认占满高度。按旧版错误说明+重试的紧凑结构收缩，安全错误编号与复制能力保留。新增失败回归观察到600高错误卡占满600视口，以及宽评教仍存在DataTable；日志 `/tmp/ubaa-old-o3b-red.log`。本批实现/新原生验收正在进行，不沿用O3-A图声称完成。

### O3-C子菜单与当前页标题

按已读冻结三个HomeScreen恢复有查询能力宿主的根菜单：博雅3项、图书馆2项、研讨室3项。点击子卡发出既有FeatureQuery并压入当前只读返回栈；顶栏标题来自实际可见帧（冻结 `MainAppScreen.kt:688–708` 的课程/作业详情与预约子页映射），返回保留根查询草稿。菜单未展示预载数据，所以路线图标显示未确定；子页以该页读取结果实际路线为准，迟到结果不能替换已返回菜单。现有无查询回调的独立只读组件继续呈现宿主提供的快照，避免造出不能操作的菜单；生产宿主一直提供查询能力。

门锁菜单采用“门锁状态”，保留旧版第三项位置并准确说明现有公开available能力；不承诺明文密码。研讨室顶层图标按 `AdvancedFeaturesScreen.kt:47` 使用DateRange，说明改为研讨室语义。根菜单的查询面板保留完整参数入口，本地结果搜索只在结果页显示；手动应用根查询同样进入子页，不覆盖菜单。5项菜单/草稿/实际路线/迟到响应测试已RED，日志 `/tmp/ubaa-old-o3c-menu-red.log`。

默认子页复用匹配的预载快照，含首次错误和加载状态；打开菜单不隐式重试，显式应用仍读取。缓存依据是冻结 `BykcViewModel.kt:95–111`、`LibBookViewModel.kt:63–83`、`CgyyViewModel.kt:85–100` 的ensure/initialLoadedOnce保护。对应新增重复读取及首次错误RED记录后，13项菜单/读取导航聚焦回归通过（`/tmp/ubaa-old-o3c-menu-green-r3.log`）。UI完整首轮因测试导航辅助未进入新增子页失败，修正辅助后的第二轮仅三张旧根页golden不匹配；三个新候选已逐图观察，正在原生验收，未声明P4-D完成。


### O3-D3博雅操作链复核（2026-09-09）

旧`BykcCoursesScreen.kt`整卡/状态/教师/地点/时间/人数条对应`features/bykc/course_card.dart`；`InfoRow`原值单行省略，当前列表教师地点同样单行，详情完整换行。`BykcTimeFormatters.resolveSelectTimeDisplay`对应当前开始/截止选课单时间，不改变任何资格。四字标签随字体自然宽度，避免固定56宽在1.3字体拆字。

`BykcChosenCoursesScreen.kt`对应紧凑已选卡与完整考勤/考核/零分；点击到独立课程详情，prepare取消仍回该详情，顶部返回列表。私有显示帧只使用同查询recordId/courseId唯一记录，刷新缺失后父列表也不复活。`BykcStatisticsScreen.kt`保持总净有效次数卡→分类三列表，null/0及服务器达标结论独立保留。原全部查询从顶栏按需面板进入，默认四状态和草稿保留；不增加常驻工具栏。

公开Bridge缺少未选课程类别/适用校区及完整详情介绍等字段，见`source-parity-ui-bykc.md`，不能假称恢复了这些旧能力。本批原生合成证据为`old-d3-native`，P5完整写入与全目标验收仍未完成。

### O3-D1图书馆操作链复核

完整核对冻结 `LibBookReserveScreen.kt` 的楼馆/楼层FilterChip、分区卡、四列座位与预约摘要，以及 `LibBookViewModel.kt:82–205` 的换楼馆/楼层/分区清除旧时段座位顺序。当前公开投影仅剩楼层数量，必须先保留楼层实体和请求日期，不能从标签或位置猜ID。原五类API参数、独立token与失败语义维持 `source-parity.md` 图书馆九列合同；本轮补读 `LocalLibBookApi.kt:54–535` 与backend Mock测试，核对精确请求和parser，examples固定源无同协议。时间关联依 `source-parity-ui-reservations.md` 保留限制；不恢复旧版未经当前合同支持的自动跨日期配对。

D1当前实现：五类Libbook公开投影保留嵌套楼层/原始时段/状态与读上下文，actions保持独立。预约页沿旧版在同一页面显示楼馆/楼层选项、分区和四列座位；可见时按原次序加载初始分区/详情，不增加每层顶部返回。隐藏页面不扩展后台读取链，换父项清除旧座位，楼馆重查保留仍存在的原选择。时段仅只读列示，从分区进入座位查询时日期清空且必须明确填写，禁止空值静默退回今天；手输参数仍在顶栏面板。新验证已实际发现并修复空日期回落、隐藏页额外请求和picker采用兼容假ID问题。输入面板宽度与长名称省略、卡片10间距按手机原生观察继续调整。


### O3-D1b分区地图与时段草稿

冻结 `LibBookReserveScreen.kt:508–539` 精确列出29个内置分区地图；`:343–415` 全屏弹窗提供缩放、拖动、重置与关闭，`:486–506` 限1–5倍且回到1倍清平移。图片是静态座位分布，不代表实时可约；本轮按原ID白名单复制原资源到新UI包，冻结目录不改。图中文字含“可用/点击”属于静态原图，查看器明确说明颜色非实时、预约须在页面中选座，避免把图片当可写目标。

时段选择只回填当前公开slot.id/start/end三字段，日期仍清空且必须明确输入；不据availableDates第一项推关联。此为少手填的必要优化，未更改公开API、资格或协议。地图子能力归LIB-03，时段草稿归LIB-04；84编号不变。


### D2研讨室下一批映射

依据冻结`CgyyReservePickerScreen.kt`的校区/日期/站点→房间时段表→下一步，恢复自然选择而非一屏手填ID；完整查询仍在右上面板。矩阵保留非允许时段，固定房间列便于手机横向浏览，已选才出现下一步。订单沿`CgyyOrdersScreen.kt`地点/状态/时间、主题/用途、详情/取消顺序；公开合同未提供的驳回正文与锁码不添加。当前仍是实施前映射，未宣称运行通过。

### O3-D4阳光旧首页复核（2026-09-09）

逐段旧源码与协议对应见`../migration/source-parity-ui-ygdk.md`。已恢复本学期/本周文字概要→记录列表、右下新增；运动项目不铺在首页。记录按旧项目/地点/开始结束/提交时间/图片数/分享顺序，低频编号和原始状态到详情后仍可搜索。首页加载更多保留前页，独立记录视图仍保留page/size查询。搜索与查询仅右上按需面板，关闭保留草稿。三端正常链与异常矩阵已实际运行，164张合成原图分r1/r2及macOS独立实窗归档；r2只重验最后低频搜索修复的正常明暗链，不能称r2全矩阵。首页提醒开关、独立照片表单与完整写流程继续后续批次。

### O3-D5首页六来源（2026-09-09）

已完整复读HomeTodo/HomeTodoTest、HomeScreen和MainAppScreen对应段。首页恢复今日课程时间排序/简称→博雅、SPOC、希冀、研讨室、签到、阳光六来源时间混合待办；保留旧卡片来源/状态/标题/副标题/时间顺序及12间距。点击直达原详情/订单，签到只凭原typed action进入协调器确认，不继承旧直接提交。额外已选/订单/当前周缓存不覆盖领域快照；首页与详情并行互不丢结果；阳光固定路线回读只同步已有结果。

阳光提醒默认开，按账号及周/学期存储达标记忆，4/16仅为旧提醒规则；未确定当前周不猜。首页顶栏单图标按已显示来源显示单路线或混合路线，说明面板可滚动。实现和安全边界见`../migration/source-parity-ui-home.md`。617项门禁、两端各54原图、macOS16原生及6独立实窗和生产两路线8来源已取得；成绩变化通知及学业旧细节、完整写入、最终全局验收继续。

### O3-E1考试与课堂签到（2026-09-09）

按ExamScreen和SigninScreen实际源码：考试三端同一日期时间线，已结束默认收起/日期倒序，待考升序、待定随后、未安排最后；列表只保留课程/座位/时间/地点，原日期和低频字段本地详情。无服务端分页时先对全部已加载考试分组，避免本地20条分页隐藏待考。签到改20内边距横卡、右侧动作或已签图标，状态未知和缺目标仍明确说明；原typed操作进入协调器确认，不照搬旧直接提交。系统Material/Widgets/Cupertino统一中文。

当前624项门禁已通过；最后描边和自适应详情改动、三端原生r4与生产双路线验收继续中，不借用B2宽表格候选。对应source-parity-ui-academic-old.md；成绩旧统计/变化通知待E2。

### O3-E2a成绩（2026-09-09，验收进行中）

逐行来源为冻结GradeScreen/GradeViewModel/GradeScreenLogicTest，协议与缓存边界见source-parity-ui-grades.md。恢复全部成绩统计→本学期统计→Outlined课程卡；两个统计卡与全部课程共用滚动区，不做宽屏表格或常驻概览。查询/学期/已出待出仍只在右上按需面板，关闭保留草稿。主卡保留课程号/学分/课程属性/成绩类型，原始逐门绩点和其余兼容字段仍可本地查看；公开不存在的类别/考试性质/考试类型不补造。

完整未筛选学期集合独立投影，三平均沿旧公式与中点取偶；部分学期失败显示覆盖率，认证失败不显示成功统计。独立聚合按真实学期去重并发，同代合并，切换已缓存学期/成绩视图复用数据，同查询刷新仍读上游。统计加载完成后每个实际route在唯一顶栏图标中说明；不将Auto当实际路线。当前手机/macOS首轮与iPad/macOS第二轮局部原生已通过；手机最后复验、生产和独立macOS仍继续，不能以此关闭整体P4或E2b。

O3-E2a局部终态（2026-09-09）：成绩完整投影、旧统计/描边卡、全学期缓存与按需查询已实现；最终641项Flutter门禁（46/50/257/215/15/23/35）通过。三端r2各18业务场景，最后学期弹窗收缩后r3各2正常查询场景通过；两端各50张候选原图加macOS独立8图，共108PNG已逐图检查归档old-e2a-native。Direct/WebVPN Core-live与生产App均通过，9/9学期82条完整聚合，当前14待出、0已出、详情无额外读取，真实业务写入0。最后只修改选择器高度，未重跑已通过且未受影响的生产读取路径。CUA Key A输入事件限制保留，不宣称完整物理键盘通过。E2b首页变化通知和整体P5–P7仍未完成。


### O3-E2b局部终态（2026-09-09）

E2b首页成绩变化提醒已沿旧横幅实现：首次仅建立独立基线，查看/忽略消费，空集合和失败不覆盖，账号与实际路线隔离，唯一当前学期读取；无通知时实际成绩检查路线也进入唯一顶栏。最终655项Flutter门禁通过，三端各12业务场景通过，手机/平板各30及macOS独立6张合成原图共66张已逐图复核。生产Direct/WebVPN各完成14条首次基线、重读和宿主/存储实例重建恢复；文件600，实测变化0，真实业务写入0。仅证明宿主重建，不冒称完整进程重启或观察到真实学校分数更新。P4其余旧布局及P5–P7继续，完整键盘限制保留。


### O3-D1c局部终态（2026-09-09）

D1c修复缺省父标识使图书馆分区详情不自动读取的问题：仅使用当前查询返回的原唯一area.id，不回填DTO父字段；非空父标识冲突仍阻止自动进入。10项聚焦、657项Flutter门禁、三端各6场景通过，28张两端原图和macOS独立3图共31张已复核。生产Direct/WebVPN均自动完成楼馆/分区/详情、明确查询175座位和2条预约记录，真实业务写入0；先前超时不标通过。协议与资格不变，跨日期时段关联仍不猜。


### O3-E3a局部终态（2026-09-09）

E3a空教室已恢复旧楼栋分组与1–14节紧凑表格，绿色只表示原空闲令牌；无服务端分页时完整连续滚动，真实分页保留。日期/校区/楼栋/节次和搜索仅右上按需面板，学院路/沙河/杭州沿旧源码命名；原字段本地详情及检索保留。1.3倍文字必要横滚时固定教室列和楼栋标题，纵滚保持表头。661项Flutter门禁、三端最终r3各14场景通过；两端各30及macOS独立4图，共64合成原图已检查。生产两路线均158条学院路、144条沙河、楼栋节次过滤27条，详情无额外读取、草稿保留、业务写入0。macOS独立CUA操作到查询面板后持续AXError.failure，未完成该独立鼠标路径的楼栋选择，不以原生测试代称；整体P6工具输入复验继续。


### O3-E3b课表网格与本地详情（2026-09-09）

完整重读ScheduleScreen/ScheduleDateLabels/ScheduleViewModel/CourseDetailScreen：旧七日列、36节次列、每节64、至少12节、6圆角、课程12/地点11字号及原color。当前恢复相同网格，普通手机七日同时可读，大字必要横滚时固定节次列；未知时间另列且不随横滚裁切，重叠区可打开全部课程，无服务端分页时不截断20条。空周仍显示七日网格。课程低频信息在本地详情，包括credit为空时的教师/教学对象，关闭不读取。学期/自然周选择和真实周日期仍待E3c接线，不能以此批关闭SCH全流程。

## E3c 自然学期周选择与日期（2026-09-09）

旧版依据：ScheduleViewModel优先首个selected学期/curWeek周，缺少标记时退回列表第一项；当前为避免把回退项冒称“当前”而要求唯一标记，歧义交由明确选择，这是必要优化。ScheduleDateLabels使用返回的起始日（失败则结束日减6）逐日显示，不用本机日期猜测。对应源码、旧测试及双来源请求对照见`../migration/source-parity-ui-schedule-old.md`。必要优化：按用户新要求把学期、教学周、前后周和兼容手填全部收进右上按需面板，顶栏只显示原周名称；长名称单行省略，完整名称在选项中查看。当前实现：schedule_flow、week_picker及独立academic_weeks缓存；唯一项不成立则明确选择，不取第一项。换学期清周次，关闭保草稿，应用才读课程；日期元数据须与当前课表实际路线一致。三端原生各24场景及129图已检查，详见本轮证据；完整物理键盘仍未通过。


### E4 登录与账号复核（2026-09-09）

E4登录沿旧32边距、学号→密码→记住/自动→登录→错误顺序；单路线图标占安全区56高顶部行，短窗口表单独立滚动，busy禁用提交/完成键/密码显隐。资料保持旧资料卡和本地遮罩优化，设置保留现有主题/统计/诊断能力、侧栏进入；当前公开策略切换不照搬旧注销语义。旧关于/外链差异仍需补齐，未关闭P4-A。

### P5-A 确认页必要保护（实施前）

冻结SigninScreen为卡片动作，LibBookReserveScreen为座位→预约摘要→确认预约，EvaluationScreen有提交遮罩和逐项结果；本轮不照搬旧直接写入，继续唯一WriteCoordinator。当前确认页仍展示drawer、首页刷新和正文重复路线，属UX-O8，不符合用户单顶栏要求。拟改为顶栏取消返回，沿同一cancel命令；提交/取消/回读中禁用返回并移除drawer，系统返回同样取消而非绕过协调器。路线只在顶栏显示intent实际固定路线，独立组件默认仍可展示路线。到期按原expiresAt自动禁用，只更新UI，不修改意图、prepare/commit参数、资格或Core期限。


### P5-A 局部实现及验收记录（2026-09-09）

P5-A统一确认页已实现顶栏取消返回、隐藏侧栏/首页刷新/正文重复路线、系统返回沿协调器取消和原期限自动禁用；提交/取消/回读中继续阻止返回，已发请求跨准备期限不误报过期。4项行为RED→GREEN，最终692项Flutter（54/52/266/232/15/23/50）通过；三端r4各20合成业务场景，手机46与平板46原图全数核对归档old-p5a-native。macOS独立computer use再次返回锁屏，本批实窗复核待补；不关闭全部P5或P6/P7。


### P5-B 独立表单局部终态（2026-09-10）

研讨室已恢复旧独立表单的已选时段→预约信息→附加选项、16外边距/12字段间距和多行内容；类型选择原key，刷新/手填仅按需打开，草稿仅当前帧内存且换账号/路线代次清除。700项Flutter（54/52/270/236/15/23/50）和just check通过；三端r3各20业务，两端各66原图共132已核对。生产双路线六读及App10用途/表单/返回只读通过，真实业务写入0。Mac独立实窗、筛选三行密度、长表头与完整写入矩阵仍未完成。

## P5-B2 研讨室时段页密度

冻结CgyyReservePickerScreen.kt的校区→日期→楼栋顺序及112房间列/86时段列/62表头/56行高为依据。旧版三行LazyRow选择器按用户最新要求统一移至右上按需面板，正文只留实际地点日期与时段矩阵；全部兼容参数保留在“更多查询”，关闭保草稿和选择。原表头并未固定，本轮固定表头及双向同步横滚属于长列表多端优化。当前对应reservation_choices.dart、reservation_flow.dart、reservation_table.dart和feature_detail.dart；无协议或资格变更。702项及三端r2局部原生通过，两端132图复核；生产及独立Mac补验以最新证据为准。

## P5-C 图书馆选择与正文对应

旧LibBookReserveScreen.kt的楼馆→楼层→分区→座位顺序继续沿用；当前将楼馆/楼层Dropdown与分区Dropdown置于右上查询面板，保原名、余量和typed ID。原分区日期/时段选择也移入面板；低频完整参数放“更多查询”，点原时段后展开参数并仅回填原三字段，日期仍须明确输入。正文只留当前楼馆/分区/日期、地图、座位和选后动作。现对应reservation_choices.dart、reservation_flow.dart、reservation_content.dart与feature_detail.dart；资料及业务合同不改。该批已12项App聚焦与3项UI/地图通过，原生及完整门禁正在复验。


## P5-D 阳光独立表单对应关系（2026-09-10）

| 旧版依据 | 必要优化 | 当前实现 |
|---|---|---|
| YgdkClockinFormScreen独立Scaffold，正文16横向/12卡片间距，项目→时间→地点→照片→分享 | 单56顶栏、宽端840内容上限、可滚动字段与52高底部确认 | write/ygdk_form.dart；首页新增直接进表单，项目在表单内弹窗选择 |
| 旧项目弹窗按items顺序，允许不设置交系统默认 | 只选择原唯一canonical action，无目标不合成默认值 | 原有有效项目与不可提交项目均保留，选择后可重新打开 |
| 旧小时/5分钟选择后使用当天日期，空时可自动随机时间 | 明确日期、24小时选择且双确认才回填；保留手填及现有严格校验，不恢复随机记录 | write/ygdk_time.dart；取消任一步保留原值 |
| 旧照片选取/可用平台拍摄、文件名类型大小与删除 | 沿当前平台照片选择能力，无公开拍摄接口不伪装拍摄；不恢复透明占位图 | 原ResizeImage预览、元信息与清除；取消更换保原照片，选择失败可重试 |
| 旧ViewModel表单字段保留 | 文字草稿仅当前读取代次；敏感照片关闭即释放，迟到不复活 | write/ygdk_draft.dart；代次变化关闭本表单与日期/项目弹窗 |

逐操作协议/字段边界见source-parity-ui-campus.md P5-D；本批没有请求、DTO、资格和写入协议改动。独立原生与生产只读仍以本批最终证据为准。
