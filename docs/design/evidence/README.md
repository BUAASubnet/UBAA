# UI 验收证据存储

按用户于2026-09-14明确的要求，运行截图、录屏等验收媒体保存在仓库外。此目录版本化原型源文件、文字说明、设备与状态元数据、清单、SHA-256和外部归档索引。应用图标、业务静态地图与golden测试基准继续作为工程资源管理。

本次归档标识为 `UBAA-ui-ux-20260914`。归档保存3,981张截图、合计1,397,627,832字节；连同配套文字和元数据共8,001个文件逐文件校验一致。仓库内的[归档索引](ui-ux/external-archive.json)记录原源码终点、清理后终点和外部清单摘要，不包含截图字节或本机私有路径。

历史文件中的 `image`、`pngSaved`、`savedScreenshots`、相对PNG路径及 `SHA256SUMS` 继续描述原采集结果。相对路径是外部归档的逻辑路径，不能据此认定图片本体仍受Git跟踪；查看原图时在归档根目录下按同一路径定位。`source-files.json` 的源码内容摘要保持有效，`systemCameraVerified=false`等验收分类不变。

归档另含原未发布提交的Git bundle与迁移校验记录，可用于恢复历史材料。此次只清理已发布基点之后的40个本地提交；历史记录中旧SHA与新SHA的关系见[提交映射](../../migration/evidence/2026-09-14-ui-ux-commit-map.txt)。映射中的不变SHA表示该提交原本没有截图差异。

后续运行须将已有驱动使用的 `UBAA_UI_EVIDENCE_DIR` 设为仓库外新建的私有绝对目录，再采集截图和元数据。审阅后仅将脱敏文字、元数据和摘要同步到本目录，图片及录屏留在外部归档。`.gitignore`对本目录常见媒体扩展名提供额外保护；不得用强制暂存绕过该规则。

截图归档只保留原有证据类别，不增加设备操作、真实读取或业务写入的通过范围。当前UI/UX目标仍按 `goal.md` 暂停。
