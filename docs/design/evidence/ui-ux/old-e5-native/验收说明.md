# P4-E5 关于页面原生证据

基点 b6ba2eef；两端r2生产源码摘要见source-files.json，保存原始PNG与逐图JSON，58张全部人工查看，未修改原生view尺寸。手机iPhone17 402×874/DPR3，平板iPadPro11M5 834×1210/DPR2，iOS26.5；每端11场景+teardown，手机45秒、平板47秒。原生数据来自明确的内存CourseworkBackend；system-version场景使用生产平台适配器读取真实安装包0.1.0+1，不包含学校真实资料。

覆盖两主题正常、版本失败重试、外链打开失败、在途禁重复和离开后迟到失败、1.3字号；返回原功能页不新增学校读取/写入。初轮自动断言通过但截图发现卡片半屏留白，因此初轮图不作为最终证据。新增顶部间距RED（62>20），修正后两端r2全部重跑；58图卡片紧随顶栏、地址换行与按钮均未溢出。1.3仅本测试文字缩放，不冒称真实系统字号设置与P6完整验收。

运行命令在apps/ubaa_flutter：flutter drive --target=integration_test/ui_about_test.dart --driver=test_driver/ui_render.dart -d <对应UUID> --dart-define=UBAA_UI_SOURCE_SHA=b6ba2eef+E5-r2，UBAA_UI_EVIDENCE_DIR为各自新空目录。随后为遵守每目录16源码限制，将原生入口移至integration_test/ui_about/about_test.dart，只改backend相对import；source-files同时记录旧入口摘要和新文件摘要，移动后analyze通过且两端独立运行成功。

独立computer use：手机、平板从真实侧栏进入关于，通过真实SystemAppInformation点击两链接，Safari完整地址栏分别核对https://github.com/BUAASubnet/UBAA与其/issues，无登录、无发送反馈。平板从系统左上返回，手机左上坐标点击未生效，改原生应用切换器选择现有应用；两端两次均回到原关于，版本与链接仍在。浏览器操作记录见主验收文档，不把本目录fake open截图冒称Safari截图。

macOS未通过：两次原生测试均Failed to foreground app，停Test starting无检查点，停止自有进程，不将INT退出0记PASS。独立运行只观察到普通功能页，鼠标/键盘未成功导航；原生系统菜单可通过AX打开/取消，但Flutter正文无响应，console=Yes，尚不能定位为锁屏或业务缺陷。Mac关于原生、独立窗口/外链及P5-D欠验继续保留。

确定性：完整Flutter715、布局0违例、just check通过；OHOS API26 ArkTS/HAP与动态库通过，仅无签名构建，不代表OHOS设备外链。P4其他页、P5全部写入矩阵、P6/P7尚未完成。
