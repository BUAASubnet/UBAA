# P5-F 拍照接线阶段保存

整理日期：2026-09-14。用户要求暂停目标，本批保存已有实现与证据，不关闭 P5 或最终验收。

Android 已沿冻结旧版接入系统相机预览委托，在内存压缩 JPEG 92，并限制输出为 10 MiB。可选拍照能力经 Platform、Host 传入共享阳光表单，保留业务资格、选图与拍照互斥、取消保原图、错误脱敏和退出后迟到结果保护。来源见[照片对照](../../../../migration/source-parity-ui-photos.md)。

目录中的原生截图产生于 2026-09-12，源码标记为 `5d988362+P5F-r2`，基点为 `5d988362817555251f1d4138904e62a41daa367f`。本次核对 `source-files.json` 的 17 个源码 SHA-256 与当前文件全部一致；`visual-review.json` 的 20 个图片摘要也全部一致。

| 证据 | 范围 |
|---|---|
| 手机 | iOS 原生视口 402×874、DPR 3，明暗各 5 张 |
| 平板 | iOS 原生视口 834×1210、DPR 2，明暗各 5 张 |
| 场景 | 拍照入口、取消保图、失败保图、成功替换、退出重入为空 |
| 数据 | `synthetic-sports-synthetic-camera`；相机与业务均显式内存注入 |
| 真实相机 | 全部元数据为 `systemCameraVerified=false` |

本次以审阅拼图检查全部 20 张归档图，并单独打开此前与 r1 不同的 `phone/light-camera-entry.png`。画面为合成表单和纯色色块，未见真实账号、真实照片或原始响应。保留原始图片、元数据和历史 `visual-review.json`，不改写当时的审阅状态。手机两主题 `reentered-empty` 图的照片按钮行下缘未完整显示，本图只能证明照片为空；完整滚动可达性仍需恢复后实操复核。

本次提交前检查均针对这 17 个源码文件未再变化的候选：

| 命令 | 结果 |
|---|---|
| `just refs` | 退出 0；两个冻结引用保持不变 |
| `just check-sensitive` | 退出 0；整理前 9120 个候选文件通过扫描 |
| `just check` | 退出 0；包含结构、合同、Rust 检查与 CLI `binary_e2e` 16 项通过 |
| `just flutter-check` | 退出 0；格式、analyze 和七包 723 项测试通过 |
| `just flutter-build platform=android-apk mode=debug` | 退出 0；生成 Debug APK，仅构建 |

Flutter 计数按脚本顺序为 Domain 54、Platform 59、App 270、UI 248、Bindings 15、Host 23、Flutter 宿主 54。原生相机测试入口为 `apps/ubaa_flutter/integration_test/ui_photos/camera_test.dart`；本轮没有重跑设备集成，也没有执行真实读取或学校业务写入。

仍待完成 Android 真实系统相机操作、手机/平板真实系统选图、macOS 独立交互欠验、Windows/Linux 照片接线及 OHOS 设备验证。既有截图只证明合成 UI 场景，不证明这些平台能力已实际验收，也不覆盖各领域完整写入、生命周期和最终三端矩阵。
