# P5-E 系统图片选择接线与表单可达证据

基点f9115633。四端原生实现已接线：iOS14+ PHPicker、macOS11+ NSOpenPanel、Android GetContent、OHOS PhotoViewPicker。原生通道只返回图片字节、合法展示名与实际MIME，不传路径/URI，不写原文件或持久缓存；10MiB限制与非法名称拒绝在原生及Dart保留。仅macOS添加用户选中文件只读权限；photos=granted表示可请求系统选择，不表示获得整个相册访问权。

最终r3手机iPhone17 402×874/DPR3、平板iPadPro11M5 834×1210/DPR2，各3项（1真实平台探测+2主题表单接线）+teardown通过，手机12秒、平板11秒；Mac3项/11秒通过、8检查点。两端共16原始PNG及16逐图JSON；r2已逐张查看，r3全部PNG与对应r2逐字节相同，visual-review.json记录每张摘要。r3是重新实际运行得到的原图，没有用r2文件替代；原生view尺寸未修改。窗口稳定帧处理消除了初轮平板转场残边，旧r1图片不作为最终界面证据。

本目录图片的业务backend为显式SportsWriteBackend，平台能力来自createDefaultPlatformCapabilities，未注入MemoryPhotoPicker。测试验证新增入口→独立表单→项目弹窗→照片按钮可用→取消返回，未调用系统选择器或提交。图片不是实际选图/预览成功证据，系统选择、取消、重选及超限文件操作仍待computer use解锁后完成。

确定性：原生默认探测RED、平台错误不应伪装取消RED、失败更换保原图/大小访问说明RED均已修复；最终Flutter717（54/55/270/246/15/23/54）与just check通过。共享Apple PhotoPayload的实际Swift合成文件程序验证PNG内容MIME、原文件不变、合法/非法名称、10MiB边界、空/非图/超大拒绝，通过。Android最终Debug APK构建通过，OHOS无签名HAP与动态库通过；都不算对应设备系统选图验收。

原生运行：apps/ubaa_flutter下flutter drive --target=integration_test/ui_photos/native_test.dart --driver=test_driver/ui_render.dart -d <对应模拟器> --dart-define=UBAA_UI_SOURCE_SHA=f9115633+P5E-r3，UBAA_UI_EVIDENCE_DIR各用新空目录。Mac使用flutter test同入口、-d macos与同source定义。原生字节验证：swiftc -parse-as-library packages/ubaa_platform/native/apple/PhotoPayload.swift packages/ubaa_platform/test/native/apple_photo_payload_test.swift -o /tmp/ubaa-p5e-photo-payload-test，然后执行该文件；全部输入由测试在独立临时目录创建并清理。

真实只读：生产Direct46秒、WebVPN新隔离配置62秒通过概要11项目、首页20记录、独立表单和项目弹窗、取消返回及记录页2→1（每页5），actualroute一致，0 prepare/0学校写入。旧WebVPN配置首页恢复断言失败保留；新建本批App容器配置安全认证后通过，不推断协议变更。这些只读流程在最后本地大小说明/非法名称拒绝补强之前完成；其后r3对最新表单重渲染，未更改学校参数，最终P7仍需固定提交验收。

未完成：系统选择/取消/重选实际操作、Android旧拍照、Windows/Linux照片接线、OHOS设备验证、独立Mac交互、其余P5全领域及P6/P7。computer use多次返回Mac锁屏，已一次请求解锁并继续独立工作，不绕过锁屏，不将探测/构建/内存照片当作真实系统选择成功。


2026-09-12补验：同源码macOS真实系统选择、取消、重选、超限拒绝和清除共5项已实际操作通过，见[系统选图实操](system-picker/验收说明.md)。原r3测试自身未调用选择器，其results.json保持原范围。手机/平板系统选择仍待。
