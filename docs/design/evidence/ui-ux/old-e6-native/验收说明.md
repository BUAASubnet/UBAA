# E6 课堂签到显式查询与 macOS 补验证据

基点7459d677。旧签到横卡与生产投影不变，本批仅补原生/生产只读测试，并修复内存AcademicOldBackend未处理显式查询的缺口。原生RED在手机两个主题均显示预期1条、实际4条；旧文字搜索不能证明三个视图已验收。

手机iPhone17（62B0C851-ABFA-4169-B5B6-A2A155588931）402×874/DPR3；平板iPadPro11M5（9F9D9C27-D085-467E-86B6-2960D0082ECC）834×1210/DPR2，iOS26.5。两端各2主题完整流程+teardown，均31秒通过；各12张原图共24已逐图检查，原生尺寸未改。Mac同用例27秒通过，12检查点，无本批独立鼠标图。

流程：打开右上面板→选可签到但不应用→关闭重开保草稿与全部结果→应用仅原allowed→本地详情无额外读取→返回高级功能再进入保视图→已签到仅原denied→全部恢复未知和缺目标→刷新保原query。prepare/commit均0；不能凭展示文本重新计算资格。面板默认不占正文，手机按内容收缩、平板右上浮层；没有溢出。其他空/错/长文等状态的旧E1证据独立保留，本批不重报为全部状态新验收。

运行：apps/ubaa_flutter下，flutter drive --target=integration_test/ui_academic_old/signin_queries_test.dart --driver=test_driver/ui_render.dart -d <对应UUID> --dart-define=UBAA_UI_SOURCE_SHA=7459d677+E6-r1，UBAA_UI_EVIDENCE_DIR各用新空目录。Mac使用flutter test同入口、-d macos与相同source定义。源码摘要见source-files.json。

真实只读：两条全新App容器私有配置，经既有verify-live安全stdin使用授权.env.local，Core两路线各3认证+today2条。生产AppDirect65秒、WebVPN107秒，各检查考试三视图为空（真实考试详情未执行）、签到全部2→可签到1→已签到1→全部2，实际路线一致，本地签到详情不新增请求。production-readonly.json只保留状态、数量、路线及日志摘要，不保存账号/真实正文。所有学校业务写入0。

补验：mac-supplement.json记录关于Mac11场景42秒和阳光Mac42场景332秒的真实终态，前次失败/未推进仍按主日志留存。Android Debug APK因E5新增插件补编译121.1秒通过；不是设备验收。阳光生产Direct表单试验在38秒失败，原生照片通道未注册使新增按钮被既有能力门禁隐藏，已登记P5-E，不绕过门禁或把内存照片当生产能力。

App54、analyze、just check、refs与敏感检查通过。完整715为E5未改生产实现的最近完整门禁，本批不声称重新执行715。独立Mac、照片原生接线、P4余项、P5全领域及P6/P7继续。
