# P5-A 统一确认页局部证据

本目录仅显式合成数据。入口为 `apps/ubaa_flutter/integration_test/ui_write_confirmation/native_test.dart`，backend 为同目录 `ConfirmationBackend`，继承合成学业列表；prepare/cancel/commit/readback 均留在内存。

手机 iPhone 17（62B0C851-ABFA-4169-B5B6-A2A155588931）402×874/DPR3、平板 iPad Pro 11 M5（9F9D9C27-D085-467E-86B6-2960D0082ECC）834×1210/DPR2，系统 iOS 26.5。两端 r4 各20业务场景及teardown通过，各46张原生原图均逐图检查，无布局溢出；每图配套元数据保留实际时间/路线之外的安全运行属性。图像未改视口、未裁切。

场景为正常取消后重新准备并提交、取消失败重试、取消中、提交中、提交跨原准备期限、回读中、结果未知、业务false、typed网络异常、未提交停留过期；每种明暗两主题。结果未知只回读一次并保留不重复提示；忙碌时按钮和系统返回均被保护。

macOS r4 800×600内容原生测试20场景通过（58秒）；独立computer use复核返回设备锁屏，故无本批macOS独立原图，不能据此关闭该视觉/鼠标验收缺口。此批不是全部领域写入验收，也不是P6/P7最终固定SHA验收。

原生命令：锁定Flutter 3.41.9，工作目录 apps/ubaa_flutter。iOS以 `flutter drive --target=integration_test/ui_write_confirmation/native_test.dart --driver=test_driver/ui_render.dart -d <上述设备UUID> --dart-define=UBAA_UI_SOURCE_SHA=3698f27a+P5A-r4` 运行，UBAA_UI_EVIDENCE_DIR分别指向本地old-p5a-native-phone-r4和old-p5a-native-ipad-r4新目录。macOS以 `flutter test integration_test/ui_write_confirmation/native_test.dart -d macos --dart-define=UBAA_UI_SOURCE_SHA=3698f27a+P5A-r4` 运行。
