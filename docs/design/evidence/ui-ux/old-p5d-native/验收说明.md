# P5-D 阳光独立表单原生证据

基点3c43370f，最终生产源码摘要见source-files.json。本目录276张PNG均为原生模拟器显式合成数据，逐图检查完成；没有真实个人信息或学校业务写入。真实账号仅Core认证与概览/记录只读，未归档真实截图。

手机iPhone17（62B0C851-ABFA-4169-B5B6-A2A155588931）402×874/DPR3；平板iPadPro11M5（9F9D9C27-D085-467E-86B6-2960D0082ECC）834×1210/DPR2，iOS26.5。完整r1每端42业务加teardown通过，手机423秒、平板422秒。r1图片检查发现分享卡片重复标题，按旧版单行标题加说明修复，并将日期标题收为“选择开始日期”。最终r2每端16业务加teardown通过，各310秒；分批复验，不声称完整r2重跑所有42场景。

归档每端138图：r1未受影响的首页/记录状态42图，r2的完整表单与正常查询96图。r1表单图仍在本地历史目录，不作为最终证据。原图未缩放，逐图JSON保留原sourceSha；源码摘要为最终r2，r1与r2的表单差异及修复证据见主验收日志。输入态含原生viewInsets但不包含完整OS键盘画面，不据此关闭P6键盘验收。

七类合成写入状态各两主题：成功、结果不确定、回读失败、上传失败、取消失败后重试、过期及提交中；SportsWriteBackend仅内存。业务确认、单次commit、原Direct概览/记录回读和新增合成记录41实际断言。正常流程另覆盖独立页→项目选择、不可提交项目禁用、空字段拦截、日期时间双确认、返回草稿、照片清除、分享往返、准备取消与原查询分页。原42矩阵还覆盖首页/记录空、首次失败、局部失败、stale、加载、1.3长文和大量列表。

命令在apps/ubaa_flutter，Flutter3.41.9：`flutter drive --target=integration_test/ui_sports_test.dart --driver=test_driver/ui_render.dart -d <上述UUID> --dart-define=UBAA_UI_SOURCE_SHA=3c43370f+P5D-r1`；r2改source为P5D-r2并增加`--dart-define=UBAA_UI_NORMAL_ONLY=true`。UBAA_UI_EVIDENCE_DIR每次用新的本批目录，未修改native view。

macOS：r1旧测试寻找不存在的结果页返回按钮，已修测试并停止；r2新进程无法置前且停在首场景，CUA两次确认Mac锁屏，已停止自有进程。手动INT退出0不算PASS。当前候选仍须重跑macOS全部原生测试、独立窗口/鼠标操作和生产App双路线只读（入口已更新且analyze通过，尚未执行）。构建与两端图片不替代这些验收，Goal保持执行中。
