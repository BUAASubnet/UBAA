# 现有 UI/UX 审查与问题记录

日期：2026-09-08。基线源码 `e6e0e3c5`，仅巡检测试接线有增量。用户群体为大学生；高频任务假设来自合同与功能结构，没有进行用户访谈，不宣称用户研究结论。

## 已实际观察的优点

macOS 800×600 内容窗口、深色、显式合成 backend：登录表单居中且有密码显示切换、记住密码/自动登录与路线入口；统一 Material 3 和蓝紫主色有辨识度。首页文字/图标/状态共同表达，卡片有明确实际路线。课表、考试、成绩和博雅页面保留查询、白名单字段与返回；博雅长详情可滚动至操作按钮，选课确认确实单独显示目标、实际路线、有效期与警告。没有为了视觉省略确认步骤。

## 问题表

证据类别：运行观察为 Computer Use 实际查看/操作；静态推断以具体代码为依据，需后续失败测试或运行确认。下表严重级按合同定义，未因外部依赖降级。

| 编号 | 级别/依据 | 复现与现象 | 原因/影响 | 处置与状态 |
|---|---|---|---|---|
| UX-01 | P2，运行+静态 | 首页与普通功能提供同一八卡片；高级分组需另一步 | 普通/高级不是学生任务分类，首页无突出今日任务 | 已实施今日/学习/校园/我的，12入口及手机平板原生复验通过；最终三端待齐 |
| UX-02 | P2，运行 | macOS 800×600 课表，周次标签显示省略号 | academic.dart 固定110宽，重要字段说明截断 | P4B手机r1再现；缩短学期/周次/楼层/节次标签，必填/可选提示独立，26项聚焦通过，原生r2及iPad复验通过 |
| UX-03 | P1，运行+静态 | 切换周课表后输入标“学期编码（可选）/留空使用当前学期”，应用却报“学期编码不能为空” | query_controls.dart 明确周表必填，academic.dart 提示未按视图变化 | P4B新增必填提示与缺项零查询RED后修复；原校验不变，原生r2及iPad复验通过 |
| UX-04 | P1，静态 | 返回/跨800断点重建详情控件 | shell.dart 窄body与宽Row分支无稳定状态锚点，query_controls.dart 无初始query与draft恢复，输入可丢失 | 回归测试确认；P3 同一状态容器保留 draft/筛选/滚动 |
| UX-05 | P2，运行 | 成绩92、绩点3.8、学分4与课程ID同级；课表时间地点排在编号后 | 通用字段列表缺领域优先层级，空间使用低效 | P4B学业已用typed分日课表/考试时间座位/成绩卡与宽表/教室节次；其他领域后续批次，最终三端待验 |
| UX-06 | P1，静态 | 手机复杂查询和软键盘时内容Column上方Wrap不受滚动约束 | query_controls + detail_list多层固定搜索与Expanded挤占正文 | 原生手机复现中；筛选抽屉/滚动上限、保持提交可达 |
| UX-07 | P2，运行 | 博雅详情底部直接展示“Core 校验”等实现词汇，禁用退选提示与签到邻接 | 用户不易分清具体哪个操作受限 | 资格原因绑定对应操作，用户文案说服务确认；待实施 |
| UX-08 | P1，静态 | 图书馆楼层只显示数、时段拼成文本，需手输segment | app/read/libbook.dart 丢失现有typed结构，无法完成自然选择流程 | 补公开结构支持的馆/层/区/首日时段选择；跨日期映射缺证据保持边界 |
| UX-09 | P2，静态 | 只能跟随系统主题 | host MaterialApp固定ThemeMode.system | 已实现运行时系统/浅色/深色，Host2项及手机平板原生操作通过，不改Core配置v1 |
| UX-10 | P2，静态 | 场馆只显示allowed时段 | App投影过滤非allowed，可用性与写资格混在一起 | 保留不可约/未知信息，写入口仍typed allowed；待源码对照和RED |
| UX-11 | P2，静态 | 个人资料只有姓名账号 | 公开学校标识未投影 | 按已证白名单展示必要字段，不添加无来源院系 |
| UX-13 | P2，运行+设计核对 | B2手机周查询应用后筛选仍占首屏较大区域，无法手动折叠 | 小于600的设计要求筛选折叠面板；现仅支持外层滚动，尚未实现收起操作 | P4-A共享余项补默认展开、用户可收起的面板；折叠不清草稿或发请求，后续原生复验 |
| UX-14 | P1，确定性RED | 本机清除确认期间换账号，旧确认仍调用清除回调 | ProfileView确认等待后缺失页面生命周期判断，可误作用于后续账号 | expected0/actual1失败后加context.mounted防护；4项聚焦通过，原生本地确认与整批A待复验 |
| ENV-01 | 工具观察，非产品定性 | 原生CUA typeText在快速切换输入框时出现截断，AX setValue未驱动Flutter控制器 | 输入树与渲染不一致，不能据此宣称表单参数通过 | 用原生integration确定性输入验证，人工点击与截图单独计；待复核工具输入 |

## 任务点击基线

从首页：课表/考试/成绩/博雅入口各1次点击；进入课堂签到/场馆/阳光/评教需高级分组+卡片共2次。课程/订单详情再选查询视图、选或输入ID、应用，至少另3次操作；图书馆多层手输参数会增加操作。前四项入口已实际打开，其余点击数为静态下界。截图目录 `evidence/ui-ux/` 中 `p0-*-macos-dark.png` 为实际 App 合成渲染，不是原型或上游证据。

## 审查进度

已实际观察：登录、主页及十二领域主页；博雅列表/滚动/确认/取消；阳光表单和缺项错误；我的/诊断/清除确认/取消。iPhone 7项宿主fake集成复验通过，涵盖十二入口、typed查询、十种写操作。其余主要子视图的逐页视觉、手机/平板最终布局仍待完成，不将该有限基线称为全页面验收。P0/P1 严重问题尚未清零。

补充 ENV-01：macOS 巡检 stderr 出现 Flutter HardwareKeyboard 的物理/逻辑按键不匹配断言，与 CUA 快速输入时序相关；不修改生产键盘逻辑掩盖工具问题。原生 iPhone 的 tester.enterText→typed query 断言已通过，人工输入完整性仍需单独核验。

## 巡检辅助 backend 的待修项

- TEST-R1（P1）：图书馆/场馆查询当前复用固定写目标，不适合证明任意父子参数保真。P4前构造匹配查询的合成targets并验证两个不同查询。
- TEST-R2（P2）：many场景返回40条却size20且hasMore=false；补真实切片和博雅服务端元数据。
- TEST-R3（P2）：Judge批量与签到/评教过滤为固定内容；需按输入键顺序与提交状态派生。修复前这些辅助场景不计完整交互PASS。

### 修复跟踪

TEST-R1/R2/R3 已经7项契约RED/GREEN及独立复审关闭；原P0截图中的历史缺陷不回写为不存在。UX-04 已通过5项widget状态连续性测试修复，macOS实际窗口复验被锁屏阻塞，手机/平板原生续验已通过；最终旋转/分屏尚待补。UX-01/09实现已复验；其余UX项尚未完成，不降低优先级。

P3A自评补充：原生手机发现入口卡空白偏多，188高收紧为156，路线信息仍在功能详情，宽主体摘要三列。重渲染手机38图及平板38图已检查。iPad应用方向请求未触发原生旋转（1668×2420保持竖向），横屏保留失败，拟用原生XCTest设备方向事件补验；不关闭分屏或改变测试view。

UX-12（P1，原生观察+确定性RED）：已打开详情就地切换深色，课程title/subtitle仍保留浅色TextStyle而融入背景。r6/r7原图与p3b-theme-color.log共同确认；外层显式Theme依赖后，UI颜色测试与原生r9原路径通过。修复前原图保留，不把手动主题切换后重入页面的正常路径代替原路径复验。

UX-04刷新补充已关闭本批：success/loading/stale之间搜索与滚动保留，empty不显示旧条目且搜索保留，7项聚焦与原生成绩stale操作通过。UX-06短视口溢出在widget中修复，原生稳定键盘r3及整机原图通过；其他领域长查询仍随P4检查；UX-02/03字段标签、UX-05领域层级等仍在P4，不因公共容器修复而标为完成。

P3B闭环补充：UX-04原生iPad横竖旋转已通过，分屏与macOS人工仍待；UX-06图书馆在1.3文字、稳定335逻辑像素真实软键盘下按钮完整可达，两主题整机原图已复核。不把该单一查询组合推广为所有P4表单已验收。

P4B原生r1补充：14业务场景中10通过4因横滚测试误匹配EditableText失败；不是生产协议失败。82图已检查，学业typed卡片正常；UX-02/03与stale缺旧数据解释在原生复现。新增“错误原因+以下为上次成功加载的数据”两行说明；两项针对性RED后26聚焦GREEN，准备同iPhone r2全量复验。宽成绩表课程编号与长名行高的review问题也已修复，不能用手机结果代替平板宽表。

B1最终106/112原图闭环后，UX-02仍保留iPad1.3日期框末位遮挡，B2随日期选择器调整；不将总体文字可读性标为清零。B2还需完成设计中共享学期、教室typed选择和宽布局，P4-B总状态保持未完成。

B2当前：已扩大日期框至220×文字倍率并接中文日期picker，增加共享学期、typed楼层/节次、考试成绩分组与宽屏本地详情、教室楼层导航。确定性492项门禁通过，UX-02日期末位与UX-05宽结构的原生复验尚在执行，不能提前关闭。

B2闭环：手机110/平板122图全复核，UX-02旧iPad日期末位经1.3原生专图和内部滚动范围0复验修复；学业UX-05宽信息层级已实现。UX-13共享筛选折叠与其他领域仍未关闭，最终macOS及多设备验收未完成。

A批次复验：UX-11白名单资料/遮罩、UX-13手机筛选折叠已在phone/iPad完整及补充运行通过，共79图复核；UX-14过期本机确认widget RED/GREEN与正常原生取消/确认分别记账。最终macOS、新候选全覆盖仍待，不把本批等于最终三端通过。


UX-15（P2，P4-C代码审查与确定性RED）：同页混入无presentation条目时，全部列表回退到通用builder，合法作业的直接详情和勾选入口一起消失。`coursework_content_test.dart` 混合模型用例先缺选择入口失败，再按单条保留 typed 卡与未知项原字段，GREEN；手机/平板实际复验仍待，未以widget结果宣称原生关闭。

P4-C自评续记：按课程分组、父作业内题目、配对选择和空列表全局进度已实现；390/834逻辑宽度1.3字号长文本与父返回2项通过。新增文件导致目录超过结构门禁、原App测试超过1000行，现按`features/coursework/`和`test/bridge_backend/assignments.dart`拆分，layout回到0项违例。原生执行前复查macOS仍锁屏，ENV-01保持。


TEST-C1（P2，原生前fixture审查）：新课程作业backend统一返回success，使SPOC/Judge列表和签到的empty测试未经过生产empty状态。已要求按生产返回形态补失败契约后修正；评教空待评应保留success与进度，不统一改empty。该问题在原生启动前识别，未将错误fixture结果登记为设备通过。


UX-16（P2，P4-C iPhone原生实测+确定性RED）：大量作业第一页勾选B后翻到第2页，仍保留旧滚动位置，实际首屏为judge-21卡尾与judge-22组头，跳过新页judge-20。两主题r1原图已确认；`pagination_position_test.dart` 从第一页底部选择后翻页，首项不可命中RED。现内容子树按本地页/服务端页重建滚动位置，搜索和批量选择保留在外层；同页刷新与父页返回key不变。新用例和原导航连续性合计9GREEN，手机r2原路径复验进行中，不能提前关闭原生问题。


TEST-C2（P2，原图审查）：r1中light-loading-spoc-pending与light-loading-judge-pending的PNG已显示成功内容，而JSON取值时仍loading，固定4秒延迟不能保证拍摄时序。辅助backend已改显式保持/释放加载，须在takeScreenshot完全返回后再释放；新原生复验待执行。这两旧图不计加载态视觉通过。

C原生r2续验：翻页两主题many通过，root检查dark-page-two已从judge-20首项开始，原生UX-16本路径已修；当前另有两正常case在父返回后搜索B失败。专项日志显示输入框仍是A且只匹配A，先定位测试输入/失焦时序，不凭找不到B推断父缓存丢失。最小UI和真实App控制器402×874确定性复现均通过，原生诊断仍在继续。


## 旧版基准O1/O2复查（2026-09-08）

- UX-O1：四导航、默认常驻搜索查询和重复返回已替换；UI182、手机/平板各56张原生图证明当前共享骨架。旧课程/预约领域仍需O3逐页继承，不因此全部关闭UX-05/08/10。
- UX-O2：第一轮查询面板固定85%高度，少量字段仍占大半屏。新增高度RED后改为内容自适应加滚动上限；第二轮两端全部查询图已检查。
- UX-O3：当前未选中也显示批量区、确认页重复标题；两项RED后改为选择后显示及单标题，业务确认保护仍通过。
- UX-O4（已修且macOS实际窗口复验）：暗色侧栏账号头采用浅primary背景配默认浅文字，实际原生图可见对比不足；用成对表面/文字颜色修复。
- UX-O5（已修并复验）：真实SPOC的score在冻结旧版两页面均标“分值”，当前typed卡标“成绩”；不能误导为已获得分数。只需展示标签修复，协议字段不改。
- UX-O6（待修）：旧首页课程卡只用课程名/时间/地点，当前额外编号与较大垂直间隔；研讨室/图书馆/博雅原有根子菜单尚未恢复，O3需逐页完成。
- LIVE-O1：WebVPN生产App首轮SPOC为upstreamChanged；同会话第二轮恢复并全十二根页读取通过，无显式刷新触发。保留两轮结果及实时数据变化，尚未定位到稳定协议缺陷，不为通过UI擅改认证/解析。

- UX-O7（已修并三端复验）：O3-A手机课程原生复核中，教学评教仍是纵向大卡片，每项常驻单门按钮和展开行；冻结 `EvaluationCourseItem` 是选择框、课程、教师、状态的紧凑横向行。后续按同一信息结构收紧，保留单门/批量与未知资格原因。
- UX-O8（P5待修）：合成签到/评教确认页面左上仍出现侧栏图标；手机确认阶段应有明确取消/返回语义，并沿WriteCoordinator执行取消，不绕过提交中保护。

- UX-D3-01（已修并复验）：博雅原生r1长教师/地点和固定56标签撑高卡片、1.3字体拆四字标签；沿旧InfoRow列表单行省略、详情标签自然宽度，r2手机平板和macOS实际检查通过。
- UX-D3-02（已修并复验）：已选详情刷新为空后返回父列表复活旧记录；宿主RED后同步同查询祖先快照，UI/宿主及原生loading/empty路径通过。此显示缓存不替代写入回读权威。
- UX-D3-03（P5待补）：合成签到/签退确认摘要简写未包含动作方向/时间窗。已核实生产prepare已有该摘要，不能把合成简写判为生产协议缺陷；需P5补忠实fixture并完整提交回读复验。

- UX-O9（已修并三端复验）：原生首次失败时FriendlyErrorCard内部Column撑满高度，造成整页大红卡；应按错误说明与重试内容收缩，仍保留安全错误码与复制能力。


O3-C原生复查：首轮iPhone/iPad菜单与子页导航测试均退出0；iPhone42图逐图观察发现辅助InspectionBackend仍用“合成体育馆／羽毛球场／体育锻炼”文案。它们是合成数据，非学校返回；已按研讨室示例修正，重新运行两端，不把错误示例当产品业务依据。用户可见站点/订单/取消标题的残留场馆称谓同时收敛，Core/Bridge标识与原始业务响应不改。子页仍有大通用字段卡、统计未恢复旧表等P4-D缺口，不能以根菜单修复关闭这些问题。


D1问题续记：
- UX-D1-01：宿主回归发现隐藏图书馆页遇首页刷新后多查areas/detail（预期1、实际3次），新增RED后以可见性阻止后台链，6项宿主回归通过。
- UX-D1-02：从没有日期关联的分区时段打开座位查询，空日期仍默认今天；RED后为该入口显式要求日期。首次修复的SnackBar遮住底部应用按钮，改为日期框内联错误，原路径通过。
- UX-D1-03：面板picker仍取兼容展示假ID；typed值RED后优先使用公开投影ID。长typed ID又触发42px横向溢出，约束picker宽度并保留完整tooltip，复验中。
- TEST-D1-01：原生r1两主题“我的预约”因合成backend未归一默认page=0而报错；补默认页RED，按生产一基页码和真实切片修正。另首次错误重试用例误找FilledButton，实际是TextButton；不修改产品按钮迁就用例。r1共4失败、34图已逐场景检查，r2进行中。
- UX-D1-04：原生长名称芯片末端裁切、分区卡片粘连、时段编号标签省略；分别补单行省略、10间距与输入宽度，必须同端复验。UX-O8确认阶段侧栏图标仍属P5待修。


### O3-D1b复验补记

旧版29分区静态地图已迁移，原作者等元数据未复制；地图的颜色不能授予实时预约资格，未知分区明确无图。点击时段只回填原始三字段，未建立不存在的日期关联。手机和平板各58图全部检查，未见本批地图控件溢出；正文搜索面板默认隐藏。macOS首轮两项滚动辅助误选20像素文本滚动区，修复定位后18项通过；独立实窗仍受锁屏阻塞。原确认页drawer问题UX-O8保留到P5处理。


### O3-D2实际修复与剩余项

旧版研讨室选择表已恢复，全部公开槽位显示，写资格独立；首站点queryDate沿读取上下文填入关联草稿。原生实际发现选中格前景对比不足、表单字段紧贴、订单按钮错位/标题提前折行、门锁重复标题，均已修复并在原环境复验。macOS锁屏现已解除，本批独立6图与手动操作完成。暂不关闭P5用途选择、表单草稿与所有异常写入闭环；UX-O8确认页drawer仍需统一修复。长楼栋芯片可横向浏览，房间长名用Tooltip；P6需继续覆盖旋转/分屏与表格滚到底后的时间上下文。

## UX-O9 研讨室订单首页漏读（2026-09-08生产只读发现）

严重级别P1。原App将零基默认page0改1，真实账号15条全部在首批而显示空页。冻结API/ViewModel、Local透传与Core-live一致支持零基。修复App保留原始0、显示pagination为number+1，输入和前后页反向转换；固定路线取消回读仍原始0。App35/宿主5与574项门禁GREEN、手机平板原生通过；生产直连已恢复15条及详情，WebVPN/macOS原生终态继续。未修改认证或资格。

UX-O9补验：新隔离会话下Direct r1/WebVPN r2生产原生分页与详情均通过；完整页码请求/显示换算缺陷已修。另保留CUA物理输入未可靠生效的P6核验项，不能用tester.enterText证明物理键盘路径正常。

### O3-D4复验问题

- UX-D4-01（已修并复验）：阳光低频字段移入详情后搜索不能匹配原记录编号/图片数量/state；补共享搜索值，UI RED→GREEN及三端正常链复验通过。
- UX-D4-02（已修并复验）：写后已有固定路线记录回读没有接到旧首页，页面仍空记录；宿主行为RED后仅传同修订的回读快照，未增加Auto读取或改变协调器。
- UX-D4-03（P5继续）：当前照片表单仍为旧候选Dialog，字段紧贴且小屏需要滚动；按冻结独立表单补日期时间选择、间距及草稿，不以现有prepare/cancel通过代替完整P5。
- UX-D4-04（验证工具限制）：CUA中文键入/粘贴时AX值与Flutter实际画面不一致；没有据AX声称中文过滤成功，也不修改产品迎合工具。原生Flutter输入与过滤已通过，实际物理键盘仍属P6。

确认页返回UX-O8仍未关闭；阳光提醒须与首页六来源共同实现，不能放一个不生效开关。

### O3-D5修复与继续项

- UX-D5-01（已修并复验）：首页缺四类待办且作业点击只到领域根；按旧六来源时间规则及具体查询目标恢复，三端与生产只读通过。
- UX-D5-02（已修并复验）：独立首页默认请求可能被后发详情查询丢弃而永久loading；追加行为RED后按首页自己的修订与生命周期接收结果，不覆盖领域当前查询。
- UX-D5-03（已修并复验）：首页多来源实际路线仍显示未确定；按已显示来源聚合单图标和说明，短窗口96像素溢出RED后加滚动，三端复验。
- UX-D5-04（已修并复验）：原生待办卡纵向相接，旧版为12间距；恢复间距后手机r3/平板r2共108图逐场景复查。
- UX-O8补充（P5待修）：从首页发起签到确认时，左上仍为drawer且出现首页刷新；与既有确认返回问题共同收敛，完整操作状态尚未验收。
- 后续细节：Material默认导航菜单tooltip仍为英文、阳光提醒卡到记录标题间距偏小；随共享本地化与P5阳光独立表单修复。订单缺格式化日期字段时退回ISO字符串，随研讨室表单/详情批次完善显示，不从字符串授予写资格。

### O3-E1考试与签到复核（2026-09-09）

旧源码已逐段阅读并建立source-parity-ui-academic-old.md。UX-E1-01：基点考试本地20条分页在过去考试多时隐藏待考课程，已改全已加载结果日期分组及已结束默认收起，真实服务端分页保持。UX-E1-02：签到原竖排Chip/按钮/展开项占高，已恢复20内边距横卡、右动作/已签图标；未知资格提示不删。UX-E1-03：系统菜单tooltip英文已通过宿主中文委托修复，包含系统日期控件。

手机原生r2全60图实际复核发现两项纯视觉问题：考试Card.outlined被共享CardTheme无side形状覆盖，已显式继承旧描边；共用学业详情强制0.6窗口高度留下大块空白，已改为内容自适应、最高0.65窗口可滚动。最后代码正在三端r4复验。确认页drawer仍属UX-O8/P5，不因这批prepare/cancel通过而关闭。签到全部/可签/已签三种查询的最终原生全覆盖仍在P6；本批正常列表和本地筛选不能代替三个显式查询。

### O3-E2a成绩初轮复核（2026-09-09）

旧成绩页的完整统计在首版实现中缺失，且宽屏换成表格、已出待出分组与本地20条分页不符旧结构。已新增完整集合投影和独立聚合，恢复旧滚动顺序与卡片；搜索课程号等低频typed字段的真实RED已修，不从筛后列表计算统计。首次聚合/切换缓存RED与路线代次测试均已通过。

手机r1共48图已逐张检查：面板关闭无查询栏，统计与卡片同滚动区，长文本1.3未见RenderFlex溢出。异步统计完成的个别截图先于顶栏下一帧：新增widget状态检查通过，原生等待正常250ms渲染且断言混合路线后，macOS/iPad r2通过；仍需检查这轮全部图并完成手机复验。全门禁首轮被两处新增测试入口的括号lint阻断，已修复并重跑；不是产品运行失败。

E2a独立macOS复核补记：8张原图与实际鼠标导航/详情/学期选择/主题切换完成。发现学期选择器仅2项却固定528高，新增RED后改为自适应，8项选择器回归与原窗口复验通过；三端最后正常查询r3复验中。CUA粘贴未输入，typeText(grade)仅落入gra，Key A抬起logicalKey与按下不匹配；只确认gra筛选与关闭重开保留，不标完整键盘通过，不改Flutter事件协议迁就工具。

O3-E2a局部终态（2026-09-09）：成绩完整投影、旧统计/描边卡、全学期缓存与按需查询已实现；最终641项Flutter门禁（46/50/257/215/15/23/35）通过。三端r2各18业务场景，最后学期弹窗收缩后r3各2正常查询场景通过；两端各50张候选原图加macOS独立8图，共108PNG已逐图检查归档old-e2a-native。Direct/WebVPN Core-live与生产App均通过，9/9学期82条完整聚合，当前14待出、0已出、详情无额外读取，真实业务写入0。最后只修改选择器高度，未重跑已通过且未受影响的生产读取路径。CUA Key A输入事件限制保留，不宣称完整物理键盘通过。E2b首页变化通知和整体P5–P7仍未完成。


### UX-E2b成绩提醒复验（2026-09-09）

普通手机横幅首版128高，实际原图发现多占一行；高度行为RED后恢复旧横排，只有窄宽或大字才换行。损坏本地基线退出回调未处理异常，RED后改为登录页明确“未能完全清除”，不覆盖坏文件。首次WebVPN检查没有通知时漏报实际路线，RED后分开记录检查路线与通知来源。三项均经过最终三端r4复验；60张两端原图及6张独立macOS图已检查，未见本批溢出。CUA本批纯鼠标，没有扩大此前Key A输入限制的验收结论。


### UX-D1-05：可空父标识误作必需（已修复验）

D1c修复缺省父标识使图书馆分区详情不自动读取的问题：仅使用当前查询返回的原唯一area.id，不回填DTO父字段；非空父标识冲突仍阻止自动进入。10项聚焦、657项Flutter门禁、三端各6场景通过，28张两端原图和macOS独立3图共31张已复核。生产Direct/WebVPN均自动完成楼馆/分区/详情、明确查询175座位和2条预约记录，真实业务写入0；先前超时不标通过。协议与资格不变，跨日期时段关联仍不猜。生产安全计数显示2/2分区父字段为空，旧DTO默认空与旧自动导航有源码证据；source-parity-ui-reservations.md记录来源。


### UX-E3a：教室大卡、桌面常驻侧栏与大字横滚（2026-09-09）

旧ClassroomQueryScreen是完整楼栋/14节表格；B2卡片和桌面180宽侧栏、20条本地切页偏离旧层级。三项行为RED后恢复表格并统一右上查询。typed教室ID漏搜、长楼栋标题越界及横滚失去教室列分别RED→修复。最终四项聚焦行为、661门禁、三端各14、64合成原图已复核。CUA在末项详情后打开查询面板可见，后续点击楼栋选择持续AXError.failure；不宣称这条独立鼠标完整通过，原生驱动查询/草稿与生产双路线另有确证。


### E3b周课表复查

静态旧源确认新版按天大卡和宽屏240×7列偏离原七日节次网格，且本地20条分页可遗漏整周课程；公开color投影缺失。行为RED已复现并修复。大字横滚节次列离屏、未知时间列表裁切和空周丢失网格也分别RED→GREEN。原生r3手机12通过/2长文场景失败：tester.ensureVisible对已可点击课程强制横向对齐，使标题中心落在固定节次列下；修改辅助为已hitTestable不移动，当前r4复验进行中，不以失败轮宣告通过。


### O3-E3b局部终态（2026-09-09）

课表旧七日节次网格、公开原色、无服务端分页完整集合、重叠课程/未知时间保全、本地详情和空周结构已完成。667项Flutter门禁、16学业聚焦通过；三端r4各14业务，空周提示位置截图发现后RED→GREEN并仅对受影响空周r5各2原生复验。非空UI不受r5调整影响，证据分r4/r5源摘要，不冒称同次全跑。两端各38原图和Mac4独立操作图共80张通过图已复核，另1张Key A输入限制图不标通过。生产Direct32秒/WebVPN31秒，今日1、学期9、周19、周课10，本地详情无额外读取，草稿/实际路线保留，业务写入0。

macOS独立CUA已完成根页→查询面板→选学期→数字4输入→应用/收起→课程详情→底部滚动；此前AXError此次未阻止这条鼠标路径，但完整字母输入仍失败：请求COURSE-SAFE-2仅输入COURSE-SA，Key A keyUp逻辑码0x1400070004与已按下0x61不一致。未取消Flutter断言、未修改认证/资格，也不把tester.enterText视为物理键盘通过。E3c自然周选择与日期、P4其余及P5–P7继续。

## E3c 运行审查与限制（2026-09-09）

- UX-E3C-01：原生首次点击“重新获取”触发setState回调返回Future；已先RED复现，再将Future构造移出回调，三端r3各24通过。
- UX-E3C-02：r2长教学周名称被leading“本周”标记挤成窄列；几何RED后把小标记移至日期下，r3手机/平板明暗及1.3原图复验通过。
- ENV-E3C：独立CUA点击/窗口Raise反复返回noWindowsAvailable；只读系统状态为screenLocked=false、onConsole=false，不猜锁屏原因。Mac原生24场景及额外5个OS只读截窗检查点通过，不能称独立鼠标通过。此前Key A不匹配仍保留，未关闭Flutter断言或改用其他事件注入绕过。

129图已全数检查：关闭面板正文为旧网格，歧义只提示明确选择；草稿5尚未应用时原周4与日期保留，应用后同步变周5；跨学期日期来自新学期。查询面板按需打开的高度不属于默认占据半页；不会据此增加常驻工具栏。


### E4 登录与账号复核（2026-09-09）

E4三个问题已RED复现：busy可再次触发、带文字路线按钮、390高1.3字号顶部重叠；修复后14项聚焦及688项完整Flutter通过。两端各47图全部核对。Mac锁屏导致r3五个截窗检查点失败，不能计PASS；用户解锁后CUA路线/资料/主题/诊断恢复，字母输入仅a、粘贴超时、AX值与画面不一致仍保留。另一独立实例预填账号只从内存读取，密码仍需输入；CUA数字1234成功后完成合成登录和资料操作，不冒称完整字母键盘通过。尝试清除确认时工具再次报告锁屏，独立清除路径未完成，完整原生r4单列。


### P5-A UX-O8修复与保留缺口（2026-09-09）

UX-O8的drawer/首页刷新/正文重复路线已移除；顶栏返回和系统返回共用取消，取消失败留在确认页可重试，忙碌阶段禁止离开。另发现停留不重建时过期按钮不更新，以及已发请求跨期限被误标过期，均有独立RED后修复。三端各20合成业务场景通过；手机与平板各46原图已全数检查，未见本批溢出或重复工具栏。macOS独立实窗仍受CUA明确锁屏阻塞，UX-O8保留该验收缺口，不当作已关闭所有平台交互。课堂签到显式查询、其他领域表单/完整写入及P6继续。


### P5-B 原生复核与下一批优先项（2026-09-10）

研讨室表单首轮手机原图显示用途刷新和手填编号独占两行，已收进类型选择弹窗；正文仅类型按钮和小字来源。独立表单按旧顺序，返回修改保留字段/类型/附加项；生命周期测试发现late final代次延迟求值使失效未关闭，修为initState即时记录，原失败用例和原生切未认证路线均复验。课堂确认的既有安全保护继续保留。

- UX-CG-筛选密度（P1，未修）：当前时段页仍常驻校区、日期、楼栋三行52高芯片，虽然继承旧选择器，用户最新要求优先，需移到顶栏按需入口；开面板后同时出现三行芯片和原始参数属于重复操作入口。下一批优先收敛，关闭保草稿和选择，不改站点/日期/查询顺序及typed目标。
- UX-CG-表头（P2，未修）：42房间原生列表滚到底时日期/楼栋仍在而时段表头离屏；需固定时间上下文。长楼名当前横向滚动，后续在按需选项中完整显示；不把这组图片称P6最终通过。
- 截图边界：原生截图不包含系统键盘，有些输入态只显示布局避让区域，不能据此声称完整软键盘验收。个别form转场图左缘仍有前页细线，稳定展示须补截图；没有裁去边缘伪装完成。Mac独立实窗受已记录锁屏限制。

P5-B2复验：研讨室三行筛选已统一收右上，旧参数不与自然选择常驻重复；关闭不清草稿或选择、不额外读取。42房间表头固定和头/正文双向横滚通过。r1实屏发现错误提示隔开表头和数据，已改sliver顺序并完成三端r2各20业务与132图全数核对。图书馆楼馆/楼层Wrap及常驻分区卡仍会占正文，下一批按冻结LibBookReserveScreen.kt复读结果收敛；不以研讨室局部通过关闭全产品密度问题。

Mac独立补验：CUA首个卡片AX点击未导航，改实际坐标后进入；随后时段选择→右上查询→数字1输入→关闭→重开，画面确认草稿及已选保留，800×632实际应用三张合成原图归档。点击另一日期时CUA明确Mac锁屏，后续日期/站点/表单独立鼠标段未完成；不要求不安全替代工具，不把原生集成通过扩张成该段通过。

P5-C实现：图书馆正文移除楼馆/楼层Wrap、所有分区大卡和时段查询按钮，右上保旧顺序、全部原选项与更多查询。查询座位后原分区时段选项仍在，关闭未应用草稿保已选座位；当前聚焦12+3通过，正在原生复验，尚不能关闭该体验缺陷。首次App聚焦4失败是辅助定位使用不存在标签“从当前分区选择”，按实际“从当前馆区选择”修正，后续12通过，未改业务参数。

P5-C密度复验：楼馆/楼层/分区及日期时段已收右上；手机/平板全部144合成图核对，正文优先当前分区、地图和座位，查询面板内部按需滚动。42座位/1.3长文/错误/空与选后动作未见本批溢出。704项、原生分批与生产双路线只读通过；完整OS键盘、独立Mac剩余操作和全部写入仍不在此结论内。

P5-C独立Mac补验：已启动显式LibraryBackend(state=map)实际应用，CUA绑定时仍明确Mac锁屏，未取得本批独立窗口/鼠标图，不拿原生断言或黑图替代。保留P6待补；该限制不阻止后续独立实现任务。


P5-D进行中问题记录：旧新增先项目弹窗再窄表单、取消更换照片清空原图已通过行为RED并修复；新增同代次文字草稿、独立页面内项目与时间选择、关闭照片释放。709项确定性通过。macOS-r2首次启动日志Failed to foreground app；CUA getApp明确Mac锁屏，测试进程仍活但首场景未产生检查点，不能记原生PASS；独立窗口/鼠标也未执行。按实际锁屏阻塞停止本轮自有原生进程，解锁后须重跑当前候选，其他手机/平板/只读工作继续。


P5-D阶段结果（2026-09-10）：独立阳光表单按旧卡片顺序实现，文字草稿/双确认日期时间/照片取消更换保留和退出释放通过；原输入校验、canonical资格、一次prepare→commit→原路线回读未变。21阳光聚焦、注销宿主1项与完整709（54/52/270/241/15/23/54）通过；分享卡片重复标题经实际图发现后修复，最终UI241与原生受影响16场景两端复验通过。check、refs、layout和敏感检查均通过。每端42业务完整r1（手机423秒/平板422秒）+16受影响业务r2（两端均310秒）；归档每端138图，共276，全部逐图核对，见old-p5d-native/验收说明.md与source-files.json。

真实只读：新App容器内私有P5D配置，通过既有verify-live安全stdin使用授权.env.local，Direct/WebVPN各3认证+2阳光读PASS（概览11项目/记录20条），真实写入0。生产App入口已增加独立表单与项目弹窗只读浏览并启用严格命中，但本批尚未执行；Mac-r1测试定位失败后停止，r2无法置前且CUA确认锁屏后停止，无最终Mac原生或独立鼠标通过证据。不能拿两端、构建、Core-live替代。P4余项、P5其余完整流程及P6/P7继续，Goal未完成。
