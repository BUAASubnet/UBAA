# The port that the server is listening on
SERVER_PORT=

# The address and port of the client's connection to the server
SERVER_HOST=
CLIENT_PORT=

# Security
JWT_SECRET=

# VPN Configuration
# Set to true to force VPN mode, false to force direct mode. 
# If not set, the server will auto-detect the environment.
USE_VPN=
